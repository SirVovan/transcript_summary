# План: оптимизация /konspekt по расходу токенов

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Уменьшить расход токенов скилла /konspekt с ~700K до ~250–300K на 1.5 часа лекции через 10 связанных правок инструкций агентов и одну новую команду в preprocessor.py, без потери качества мастер-MD.

**Architecture:** Все изменения — в `.claude/skills/konspekt/`. Большая часть — правки Markdown-инструкций (бригадир + субагенты + спеки голоса/аудитории). Одна задача — Python (новая команда `head` в `preprocessor.py`). Тестирование — единый огневой прогон скилла на эталонном 45-минутном транскрипте в отдельной сессии после применения всех правок.

**Tech Stack:** Markdown (инструкции LLM-агентов), Python 3 (preprocessor).

**Спецификация:** [docs/superpowers/specs/2026-05-17-konspekt-token-optimization.md](../specs/2026-05-17-konspekt-token-optimization.md)

---

## Spec Reference

Каждая задача в этом плане реализует один или несколько пунктов спеки. Соответствие:

| Пункт спеки | Задача плана |
|---|---|
| #1 Перенос читательской самопроверки на бригадира | Task 3 (удаление из субагента) + Task 5 (изменения у бригадира) |
| #2 Встраивание спек в промт + сокращение | Task 3 (сокращение subagent_phase2) + Task 4 (сокращение tov_spec) + Task 5 (встраивание у бригадира) |
| #3 Sonnet для Ф1 и Ф2 | Task 5 (Agent-вызовы) + Task 6 (упоминания в SKILL.md) |
| #4 Гибридный ToV | Task 1 (preprocessor.py head) + Task 5 (логика у бригадира) + Task 6 (ссылка в SKILL.md) |
| #5 Дисциплина бригадира | Task 5 (раздел у бригадира) + Task 6 (раздел в SKILL.md) |
| #6 Заморозка Траектории | Task 5 (FROZEN в brigadir.md) + Task 6 (удаление из пайплайна в SKILL.md) |
| #7 Методичка сегментации | Task 2 (subagent_phase1) + Task 5 (Глобальный обзор у бригадира) |
| #8 Подзаголовки `####` | Task 3 (правила в субагенте Ф2) + Task 5 (шаблон сборки) |
| #9 Блоки `Ключевая идея`, `Принцип` | Task 3 |
| #10 Уровни И/М/Д + блок `Методология` | Task 3 |

---

## File Structure

| Файл | Что меняется |
|---|---|
| `.claude/skills/konspekt/preprocessor.py` | Добавляется команда `head <файл> [--tokens N]` |
| `.claude/skills/konspekt/subagent_phase1.md` | Методичка сегментации: целевой масштаб, анти-маркеры, понятие «уровень главы» |
| `.claude/skills/konspekt/subagent_phase2.md` | Большая переработка: удалить читательскую самопроверку и чтение `audience_spec.md`, сократить до ~150 строк, добавить `####` подзаголовки, новые блоки и маркировку уровней в Карте |
| `.claude/skills/konspekt/tov_spec.md` | Сокращение до ~70 строк: убрать дубли, объединить близкие разделы |
| `.claude/skills/konspekt/brigadir.md` | Дисциплина, перенос читательской самопроверки на финальный этап, заморозка Траектории, методичка сегментации (синхронно с phase1), встраивание спек Ф2 + Sonnet + гибридный ToV в подготовку Ф2, `####` в шаблоне |
| `.claude/skills/konspekt/SKILL.md` | Обновление упоминаний моделей, ToV, дисциплины, удаление «Траектории» из пайплайна сборки |

---

## Execution Order

Порядок выбран так, чтобы внутри каждой задачи правки были самосогласованны, и чтобы файлы, которые ссылаются на другие, правились **после** тех, на которые ссылаются:

1. **Task 1** — `preprocessor.py` head (нужен для гибридного ToV, на который опирается Task 5)
2. **Task 2** — `subagent_phase1.md` (изолированный файл, методичка сегментации)
3. **Task 3** — `subagent_phase2.md` (большая переработка — самостоятельный файл)
4. **Task 4** — `tov_spec.md` (сокращение — самостоятельный файл)
5. **Task 5** — `brigadir.md` (опирается на наличие Task 1 и контента из Tasks 3, 4 при встраивании в промт)
6. **Task 6** — `SKILL.md` (обзорный документ; правится последним, чтобы он отражал реальное состояние других файлов)
7. **Task 7** — финальная локальная сверка + handoff на тестовый прогон в отдельной сессии

---

## Task 1: preprocessor.py — команда `head` (#4)

**Files:**
- Modify: `.claude/skills/konspekt/preprocessor.py`
- Test: ручной запуск на существующем транскрипте (например, `transcripts/Урок 2.srt` если есть, или любом другом существующем `.txt`)

Команда нужна бригадиру: при первом прогоне новой серии бригадир берёт первые ~2500 токенов транскрипта, чтобы извлечь ToV спикера, не загружая в контекст весь чанк (~18K токенов).

- [x] **Step 1: Прочитать текущий preprocessor.py**

Команда: открыть файл `.claude/skills/konspekt/preprocessor.py` и убедиться, что доступны функции `estimate_tokens`, `parse_transcript_lines`.

- [x] **Step 2: Добавить функцию `head` после `slice_chunk`**

Файл: `.claude/skills/konspekt/preprocessor.py`. Вставить новую функцию после `slice_chunk` (после строки 206 в текущей версии):

```python
def head(path: str, target_tokens: int = 2500) -> None:
    """Печатает в stdout первые ~target_tokens токенов транскрипта без таймштамповой разметки.
    Используется бригадиром для извлечения ToV без загрузки полного чанка."""
    with open(path, encoding='utf-8') as f:
        text = f.read()
    lines = parse_transcript_lines(text)
    if not lines:
        # Фоллбэк: вернуть первые target_tokens*4 символов
        char_limit = target_tokens * 4
        print(text[:char_limit])
        return
    acc_tokens = 0
    out_lines = []
    for ln in lines:
        t = estimate_tokens(ln['text'])
        if acc_tokens + t > target_tokens:
            break
        out_lines.append(ln['text'])
        acc_tokens += t
    print('\n'.join(out_lines))
```

Тот факт, что функция печатает текст **без таймштампов** — критичен: бригадир получает чистый текст, который удобен для распознавания манеры речи, а не структуры файла.

- [x] **Step 3: Зарегистрировать подкоманду `head` в argparse-блоке**

Файл: `.claude/skills/konspekt/preprocessor.py`. В блоке `if __name__ == '__main__':` (после строки 222 — после `p_slice.add_argument(...)`) добавить:

```python
    p_head = sub.add_parser('head')
    p_head.add_argument('transcript')
    p_head.add_argument('--tokens', type=int, default=2500, help='Целевой объём в токенах (по умолчанию 2500)')
```

И в блоке диспетчера команд (где идут `elif args.cmd == 'slice':`) добавить:

```python
    elif args.cmd == 'head':
        head(args.transcript, args.tokens)
```

- [x] **Step 4: Обновить docstring модуля**

Файл: `.claude/skills/konspekt/preprocessor.py`, строки 5–10 (docstring модуля). Добавить новую команду в список:

```python
"""preprocessor.py — Предобработка транскрипта для скилла /konspekt.

Команды:
  python preprocessor.py info <файл>                                       # Статистика файла
  python preprocessor.py windows <файл>                                    # Окна для LLM-анализа границ
  python preprocessor.py split <файл> --at HH:MM:SS,...                    # Нарезка по таймингам (Фаза 1)
  python preprocessor.py slice <чанк> --from HH:MM:SS --to HH:MM:SS        # Отрезок чанка по границам сегмента (Фаза 2)
  python preprocessor.py head <файл> [--tokens N]                          # Первые N токенов без таймштампов (ToV)
"""
```

- [x] **Step 5: Запустить ручную проверку команды на существующем транскрипте**

Команда (выбрать любой существующий транскрипт в папке `transcripts/`):

```
python .claude/skills/konspekt/preprocessor.py head "transcripts/Урок 2.srt"
```

Ожидаемое: stdout содержит первые ~2500 токенов (~10K символов) текста транскрипта **без** строк с `[HH:MM]` или подобной разметкой; вывод заканчивается на середине предложения или абзаца. Если выводится 0 строк или возникает исключение — отладить функцию `head`.

- [x] **Step 6: Проверить, что прежние команды не сломаны**

Команда:

```
python .claude/skills/konspekt/preprocessor.py info "transcripts/Урок 2.srt"
```

Ожидаемое: тот же вывод, что и до правки (статистика файла, диапазон, длительность).

- [x] **Step 7: Коммит**

```
git add .claude/skills/konspekt/preprocessor.py
git commit -m "feat(konspekt): preprocessor.py head — первые N токенов без разметки для ToV"
```

---

## Task 2: subagent_phase1.md — методичка сегментации (#7)

**Files:**
- Modify: `.claude/skills/konspekt/subagent_phase1.md` (раздел «Правила сегментации»)

Цель: добавить в инструкцию субагента Ф1 явный ориентир уровня «глава курса» и анти-маркеры, которые предотвращают размельчение сегментов.

- [x] **Step 1: Открыть файл и найти раздел «Правила сегментации»**

Файл `.claude/skills/konspekt/subagent_phase1.md`. Раздел начинается со строки 49 («## Правила сегментации»). Текущий главный (смысловой) критерий — строки 51–59. Маркеры границы — строки 61–80.

- [x] **Step 2: Расширить «Главный (смысловой) критерий»**

В разделе «Правила сегментации» заменить блок «Главный (смысловой) критерий» (текущие строки 51–59) на следующий текст:

```markdown
**Главный (смысловой) критерий:**

> Сегмент = одна **крупная самостоятельная мысль автора**, ради которой он отвёл существенный кусок времени, со всеми её разворотами — концепт, демо, минусы, обход, ещё демо. Уровень «**главы курса**», а не «параграфа».

Если ты не можешь сформулировать мысль сегмента одной фразой — скорее всего, это либо два разных сегмента слиплись, либо фрагмент служебный и должен быть поглощён соседним сегментом.

**Целевой масштаб:** ориентир — **5–10 сегментов на 60–90 минут лекции**, ~10–15 минут на сегмент. Если получилось заметно больше (15+ сегментов на полтора часа) — почти всегда это размельчение, требуется пересборка.
```

- [x] **Step 3: Добавить раздел «Анти-маркеры» после «Маркеры потенциальной границы»**

В разделе «Правила сегментации», после блока про маркеры границы (после строки 73 в текущей версии — после абзаца, заканчивающегося «...а не выделяется как отдельный.»), добавить новый блок:

```markdown
**Анти-маркеры** (что **не** является границей сегмента):

- Смена формата внутри одной темы (теория → демо → ещё демо одной темы) — это **развороты одной мысли**, не разные мысли.
- Новая команда / новая фича / новый лайфхак, раскрывающая ту же мысль — это **расширение примера**, не отдельный сегмент.
- Метафора, образ, оговорка по основной мысли — часть стиля, не граница.

Признак, что ты дошёл до анти-маркера: формат меняется, но **главная мысль фрагмента остаётся той же**. Если ты можешь объединить эти 2–3 «формальных» сегмента в один и его мысль сформулируется одной фразой — это и был один сегмент.
```

- [x] **Step 4: Сохранить и проверить структуру файла**

Убедиться, что после правок:
- Главный критерий упоминает «крупная самостоятельная мысль», «глава курса», «5–10 сегментов на 60–90 минут».
- Блок «Анти-маркеры» добавлен и стоит после «Маркеры потенциальной границы», но до «Чего не делать».
- Никаких других правок в этом файле нет.

- [x] **Step 5: Коммит**

```
git add .claude/skills/konspekt/subagent_phase1.md
git commit -m "feat(konspekt): методичка сегментации Ф1 — уровень главы, анти-маркеры"
```

---

## Task 3: subagent_phase2.md — переработка (#1, #2, #8, #9, #10)

**Files:**
- Rewrite: `.claude/skills/konspekt/subagent_phase2.md`

Самая большая правка. Меняем:
1. Удаляем читательскую самопроверку целиком (#1).
2. Удаляем чтение `audience_spec.md` через Read — будет встраиваться бригадиром в промт (часть #2).
3. Сокращаем рутинные блоки — антисаботажные дубли и расширенные пояснения (часть #2). Цель — ~150 строк вместо 289.
4. Добавляем правило `####` подзаголовков внутри Текста (#8).
5. Уточняем роль Карты (тезисы, не оглавление) — различие с подзаголовками (#8).
6. Добавляем 3 новых специальных блока: `> Ключевая идея`, `> Принцип`, `> Методология` (#9 + #10).
7. Добавляем правило маркировки уровней (И)/(М)/(Д) в Карте (#10).

Поскольку файл переписывается фактически целиком, делаем это одной заменой через Write, а потом коммитим.

- [x] **Step 1: Прочитать текущий subagent_phase2.md полностью**

Это нужно для понимания, какие конкретно фрагменты сохраняются, а какие выбрасываются. Убедиться, что ничего критического не потеряется.

- [x] **Step 2: Записать новую версию файла через Write tool**

Файл: `.claude/skills/konspekt/subagent_phase2.md`. Полное содержимое:

````markdown
# Фаза 2: Субагент — Написание текста сегмента

## Роль

Ты — **спикер**, объясняющий свою мысль целевой Аудитории.
По куску транскрипта ты готовишь **Текст** (близкий к оригиналу, сохраняющий голос автора), **Карту** (извлечение оглавления из готового Текста с метками уровней) и **Ключевую мысль** (итоговая суть сегмента).

При написании держи в голове: какую мысль я доношу Аудитории в этом сегменте, зачем ей это нужно, какие термины требуют раскрытия.

---

## Входные данные

**[NAV MAP]** — полная чистовая карта всего выступления. Контекст: что было до и что будет после твоего сегмента. Не копируй из неё формулировки.

**[TOV SPEC]** — спецификация голоса и стиля. Следуй при написании Текста.

**[AUDIENCE]** — описание целевой Аудитории.

**[SEGMENT SPEC]** — запись твоего сегмента из чистовой карты: таймштамп, тема, риторическая роль, ключевые тезисы.

**[CHUNK_сегмента]** — отрезок транскрипта по границам твоего сегмента. **Единственный и главный источник содержания.**

Все четыре блока приходят встроенными в промт. Read-инструмента к файлам спек применять не нужно.

---

## Принцип постановки задачи

**Источник истины — транскрипт.**

- **Текст** — это очищенный и причёсанный транскрипт. Автор говорит сам, ты убираешь только шум.
- **Карта** — извлечение оглавления из готового Текста, с метками уровней.
- **Ключевая мысль** — итоговая суть всего сегмента, формулируется после Карты.

Карта **не** диктует Текст. Сначала Текст близко к транскрипту — потом из него Карта.

---

## Процесс работы

1. Прочитать `[CHUNK_сегмента]` — отрезок транскрипта твоего сегмента.
2. Написать **Текст**: переписать транскрипт в его границах, убирая шум по правилам ниже, сохраняя голос автора. Объём — 70–90% от транскрипта по символам.
3. Извлечь **Карту**: 4–8 ключевых тезисов из готового Текста, в порядке появления, с метками уровней (И)/(М)/(Д).
4. Сформулировать **Ключевую мысль**: 1–2 предложения, итоговая суть всего сегмента.
5. Выполнить **техническую самопроверку** и приложить её к ответу.

---

## Что произвести

### Текст

Связный нарратив от первого лица спикера, близкий к транскрипту, очищенный от шума, сохраняющий авторский голос.

**Объём:** 70–90% от транскрипта по символам. Меньше 60% — сигнал, что сократил смысл вместе с шумом.

**Что убираем (шум):**
- Слова-паразиты без смысловой функции: «эээ», «короче», «как бы» (если не указывает на приблизительность)
- Прерванные мысли без продолжения
- Технические сбои
- Прямые повторы фразы без новой функции

**Что сохраняем (это часть смысла, не шум):**
- Характерные обороты и фирменные фразы спикера
- Метафоры, образы, сравнения
- Повторы для усиления
- Эмоциональные ремарки, оговорки, переходные обороты
- Раздумья вслух, если показывают ход мысли

**Принцип сомнения:** если не уверен — **сохраняй**. Лучше оставить лишнее, чем потерять голос.

**Оформление Текста:**
- Первое лицо спикера
- Короткие абзацы по смыслу (2–4 предложения)
- **Ключевые термины** жирным при первом упоминании
- **Главную мысль абзаца** жирным
- Нумерованные списки для пошаговых инструкций
- Не упоминай другие сегменты напрямую

**Подзаголовки `####` внутри Текста:**

Если в сегменте **больше одной подтемы** (видно по [SEGMENT SPEC] — больше одного ключевого тезиса с разными темами, или по транскрипту — отчётливо разные «развороты» одной крупной мысли), членить Текст на подразделы заголовками `####`:

```markdown
### Текст

#### [Название подтемы 1]
[3–6 абзацев]

#### [Название подтемы 2]
[3–6 абзацев]
```

Источник заголовков `####` — ключевые тезисы из [SEGMENT SPEC] или естественные подтемы транскрипта. Если в сегменте одна подтема — `####` не нужны, Текст без членения.

**Специальные блоки** (использовать **только при явной формулировке спикером в транскрипте**, не по интерпретации):

> **Ключевая идея:** [фундаментальная идея/концепция, которую спикер явно выдвигает как центральную]

> **Принцип:** [общее правило/мантра, которую спикер формулирует как руководство]

> **Методология:** [структурированный подход или пошаговый алгоритм, явно сформулированный спикером]

> **Лайфхак:** [практический совет, который можно применить сразу]

> **Демонстрация:** [описание того, что спикер показывает на экране или делает вживую]

> **Важно:** [предостережение, ограничение, критическая деталь]

**Анти-саботажное правило для всех блоков:** блок используется только если в транскрипте есть **прямая фраза-маркер**:
- Идея: «главная мысль в том, что...», «суть в...», «всё сводится к...»
- Принцип: «правило простое:», «никогда не...», «всегда...»
- Методология: «делается так:», «шаги такие:», «алгоритм:», «процесс выглядит так:»
- Лайфхак: «хитрость в том:», «секрет:», «можно проще:»
- Демонстрация: «смотрите:», «вот пример:», «давайте покажу:»
- Важно: «обратите внимание:», «осторожно:», «нельзя:»

При сомнении — оставлять в обычном тексте с жирным выделением, **не выносить в блок**. Натягивание блоков на интерпретацию портит мастер-MD.

**Критерий качества Текста:** прочитав блок, человек получает то же понимание, что зритель видео.

### Карта

Извлечение оглавления из готового Текста с метками уровней. Составляется **после** Текста.

**Префикс-метки уровня** на каждом буллете:
- **(И)** — идея, принцип, концепция (стратегический уровень)
- **(М)** — методология, подход, алгоритм (тактический уровень)
- **(Д)** — действие, команда, демо, лайфхак (операциональный уровень)

Пример:

```markdown
### Карта

- **(И)** MCP — единый протокол подключения сервисов: метафора «USB-хаб для агента»
- **(М)** Установка: через Smithery или CLI с GitHub
- **(Д)** Демо Google Sheets: создать таблицу → заполнить → сделать красиво
- **(Д)** Команда `/mcp` показывает все установленные
- **(И)** MCP частично устаревает из-за нагрузки на контекст
```

Правила:
- Каждый буллет — самостоятельный тезис из Текста.
- 4–8 буллетов на сегмент.
- Порядок = порядок появления в Тексте.
- Все ключевые тезисы из [SEGMENT SPEC] должны найти отражение в Карте, если они есть в транскрипте.
- Карта **не обязана** содержать все три уровня — отражай то, что реально есть в сегменте.
- Если уровень тезиса неочевиден — выбирай **(Д)** как наиболее безопасный (конкретика всегда видна), не натягивай (И)/(М).

**Маркеры детекции уровня (по транскрипту):**
- **(И)**: «главная мысль...», «суть...», «всё сводится к...», «правило простое...», «никогда/всегда...»
- **(М)**: «делается так:», «шаги:», «алгоритм:», «подход:», «процесс выглядит так:»
- **(Д)**: «вот команда:», «делаем так:», «смотрите:», «вот пример:», «лайфхак:»

**Различие с подзаголовками `####`:** Карта = тезисы-утверждения («что сказано»). Подзаголовки `####` в Тексте = тематические разделы («о чём раздел»). Если ты замечаешь, что Карта и подзаголовки звучат одинаково — перепиши Карту в форме тезисов.

### Ключевая мысль

1–2 предложения — итоговая суть всего сегмента. Формулируется после Карты.

```markdown
**Ключевая мысль:** [1–2 предложения — самое главное из этого сегмента]
```

---

## Формат итогового ответа

В выводе блоки располагай так: **Ключевая мысль → Карта → Текст → Самопроверка**.

```markdown
**Ключевая мысль:** [...]

### Карта

- **(И)** [...]
- **(М)** [...]

### Текст

#### [Подзаголовок, если есть подтемы]

[...]

### Самопроверка

[блок ниже]
```

---

## Самопроверка (обязательный шаг)

После написания Текста, Карты и Ключевой мысли — обязательно блок «### Самопроверка». **Без него ответ считается неполным.**

Проверяет:

**1. Сохранение содержания.** Все ли смысловые блоки транскрипта в Тексте? Не пропустил ли пример, демонстрацию, оговорку, метафору?

**2. Сохранение стилистики.** Не сократил ли фразы, которые показались лишними, но были авторской манерой? Звучит ли Текст как спикер, или как нейтральный учебник?

**3. Объём.** Текст составляет 70–90% от транскрипта по символам? Если <60% — сократил смысл, перечитать.

**4. Карта с уровнями.** Каждый буллет имеет метку **(И)/(М)/(Д)**. Уровни выбраны по явным маркерам спикера, не интерпретированы.

Если по какому-то пункту находка — **исправить Текст до вывода** и повторить.

**Формат блока в выводе:**

```markdown
### Самопроверка

**Сохранение содержания:** все смысловые блоки на месте ✓ / пропускал «[что]» — добавил → ✓
**Сохранение стилистики:** авторские обороты сохранены ✓ / убирал «[фраза]», вернул → ✓
**Объём:** Транскрипт ≈ N символов / Текст ≈ M символов = X% (норма 70–90%)
**Карта:** составил из готового Текста, метки уровней проставлены ✓
```

---

## Чего не делать

- Не пиши Карту первой и не разворачивай её в Текст — это перевёрнутый процесс. Источник — транскрипт.
- Не добавляй заголовков выше `### Карта`, `### Текст`, `### Самопроверка`.
- Не пиши пояснений к своим блокам, не резюмируй после вывода.
- Не ссылайся на «навигационную карту», «субагент», «бригадира» в тексте.
- Не делай Read к файлам спек — [TOV SPEC] и [AUDIENCE] приходят в промте.
- Не выводи ответ без блока «### Самопроверка» — обязателен.
````

- [x] **Step 3: Сверить размер файла**

Команда:

```
wc -l .claude/skills/konspekt/subagent_phase2.md
```

Ожидаемое: ~150–180 строк (было 289). Если получилось больше 200 строк — есть что ещё сократить (вернуться и убрать дубли). Если меньше 130 — есть риск, что выкинули нужное (особенно антисаботажные правила) — перепроверить.

- [x] **Step 4: Проверить контрольный список содержания (read через Grep)**

Проверить, что в файле есть все ключевые элементы (через Grep с output_mode="files_with_matches"):
- `(И)` или `(М)` или `(Д)` — маркировка уровней
- `> **Методология:**` — новый блок
- `> **Ключевая идея:**` — новый блок
- `> **Принцип:**` — новый блок
- `#### `— упоминание подзаголовков
- `[TOV SPEC]` и `[AUDIENCE]` — упоминания, что приходят в промте
- НЕТ упоминаний `### Самопроверка — глазами Аудитории` (читательская удалена)
- НЕТ упоминания `audience_spec.md` через Read

Если что-то не найдено — добавить; если что-то лишнее найдено (например, остатки читательской самопроверки) — удалить.

- [x] **Step 5: Коммит**

```
git add .claude/skills/konspekt/subagent_phase2.md
git commit -m "feat(konspekt): subagent_phase2.md — сокращение + #### + блоки + уровни И/М/Д"
```

---

## Task 4: tov_spec.md — сокращение (#2)

**Files:**
- Rewrite: `.claude/skills/konspekt/tov_spec.md`

Сокращаем со 138 строк до ~70: объединяем «Сохранение авторского голоса» и «Полнота содержания» (перекрываются), убираем «Приоритеты качества» (превращаем в одну фразу), сжимаем «Принцип минимального искажения».

Динамическая часть и форматирование остаются — это активные правила, не дубли.

- [x] **Step 1: Прочитать текущий tov_spec.md**

Понять, какие блоки реально пересекаются. Уже сделано на этапе планирования: пересекаются «Сохранение авторского голоса», «Полнота содержания», «Принцип минимального искажения».

- [x] **Step 2: Записать новую версию файла через Write**

Файл: `.claude/skills/konspekt/tov_spec.md`. Полное содержимое:

````markdown
# Голос и стиль (ToV-спецификация)

Базовые правила голоса для написания конспекта. Передаётся бригадиром в промт каждого субагента Ф2 как блок `[TOV SPEC]`.

---

## Базовые правила

### Лицо и голос

**Лицо:** Первое лицо единственного числа. От имени спикера: «я думаю», «я показываю», «мы разобрали».

**Голос:** Живая разговорная речь — не академический текст, не деловой отчёт. Без канцеляризмов, без пассивного залога там, где возможен активный.

### Сохранение авторского голоса

Конспект — это «тот же спикер, но в текстовом виде, без шумов».

**Сохранять:**
- Лексику, характерные обороты и синтаксис спикера
- Индивидуальную манеру подачи, естественный ритм
- Метафоры, образы, сравнения
- Смысловые и эмоциональные акценты
- Повторения, если работают на фиксацию внимания
- Все пункты перечислений, все примеры, все пошаговые инструкции, все уточнения и оговорки

**Не делать:**
- Не переписывать от третьего лица или в обезличенном учебном стиле
- Не заменять эмоциональные формулировки нейтральными
- Не редактировать стиль «для красоты» в ущерб манере
- Не использовать канцеляризмы («таким образом», «в рамках данного», «следует отметить»)
- Не сокращать ради краткости, не объединять мысли, не «улучшать» логику
- Не превращать развёрнутое объяснение в набор тезисов

**Структура аргументации:** сначала пример или история — потом тезис. Читатель сначала видит ситуацию, потом слышит вывод.

### Что можно менять

- Удалять паразитные слова: «ээ», «ну», «как бы», «вот», оговорки без смысловой нагрузки.
- Слегка выравнивать грамматику и пунктуацию, если не меняет смысл.

**Принцип сомнения:** лучше сохранить лишнее, чем потерять смысл или голос.

**Приоритеты при конфликте:** точность содержания → авторские формулировки → стилистика → читабельность. Компактность — в последнюю очередь.

### Абзацы и переходы

- **Абзацы:** короткие, 2–4 предложения, одна мысль — один абзац.
- **Переходы:** живые связки («вот почему», «отсюда следует», «но здесь важный момент»). Не «во-первых / во-вторых».
- **Перечисления:** сохраняются списками, не объединяются.

---

## Форматирование

### Жирный текст

- **Ключевые термины** жирным при первом упоминании в сегменте
- **Главная мысль абзаца** жирным (первое или последнее предложение)
- В Карте: ключевой термин/субъект буллета жирным

Не злоупотреблять: 1–2 выделения на абзац — максимум.

### Специальные блоки и маркировка уровней

Подробно описаны в инструкции субагента Фазы 2 (приходит как `[INSTRUCTIONS]` в промте). Здесь — короткое напоминание:

- Блоки: `> **Ключевая идея:**`, `> **Принцип:**`, `> **Методология:**`, `> **Лайфхак:**`, `> **Демонстрация:**`, `> **Важно:**`. Использовать только при явной формулировке спикером.
- Карта: каждый буллет с префиксом **(И)/(М)/(Д)** — идея / методология / действие. При сомнении — (Д).

---

## Динамическая часть (заполняется бригадиром перед Фазой 2)

> **Бригадиру:** в зависимости от стратегии гибридного ToV — либо взять из `transcripts/tov_<серия>.md`, либо построить из `preprocessor.py head <транскрипт>` и сохранить файлом.

```
[TOV_DYNAMIC]
Характерные обороты: ...
Темп и формальность: ...
Фирменные фразы / обращения к аудитории: ...
[/TOV_DYNAMIC]
```

Если характерных черт не выявлено — блок остаётся пустым, работают только базовые правила выше.
````

- [x] **Step 3: Сверить размер файла**

Команда:

```
wc -l .claude/skills/konspekt/tov_spec.md
```

Ожидаемое: ~70–80 строк (было 138). Если получилось больше 90 — есть что ещё сжать. Если меньше 60 — проверить, не выкинул ли что-то критическое.

- [x] **Step 4: Проверить, что ключевые правила сохранились**

Через Grep убедиться:
- Упомянуто «первое лицо», «не канцеляризмы», «принцип сомнения», «приоритеты»
- Упомянуты блоки `Ключевая идея`, `Принцип`, `Методология` (короткое напоминание)
- Упомянута маркировка `(И)/(М)/(Д)`
- Динамический блок `[TOV_DYNAMIC]` остался

- [x] **Step 5: Коммит**

```
git add .claude/skills/konspekt/tov_spec.md
git commit -m "feat(konspekt): tov_spec.md — сокращение вдвое, без потери ядра"
```

---

## Task 5: brigadir.md — оркестрация (#1, #2, #3, #4, #5, #6, #7, #8)

**Files:**
- Modify: `.claude/skills/konspekt/brigadir.md`

Самая объёмная задача — большая часть оркестрации пайплайна правится здесь. Делаем правки в порядке появления разделов в файле (сверху вниз), чтобы было видно прогресс. **Один коммит на всю задачу** (правки в одном файле логически связаны).

- [x] **Step 1: Открыть и перечитать brigadir.md полностью**

Это нужно перед большой переделкой — убедиться, что текущее содержание знакомо, и помнить, где какой раздел.

- [x] **Step 2: Добавить раздел «Дисциплина бригадира» сразу после «Роль» (#5)**

Файл `brigadir.md`. После строки 21 (после «Адресат всех твоих ролей — целевая Аудитория...») вставить:

```markdown

---

## Дисциплина (обязательно для экономии бюджета)

Эти правила действуют на протяжении всего пайплайна /konspekt:

1. **Не использовать TodoWrite.** Пайплайн линейный, прогресс виден по тексту сообщений. Schema-reload TodoWrite (~5K токенов) не оправдан.
2. **Не перечитывать чанки после Ф1.** После получения черновых карт от субагентов Ф1 чанки используются только точечно — `Read` с конкретным таймштампом при разрешении сомнений в сегментации. Полный Read чанка после Ф1 запрещён.
3. **Не запускать дополнительные deferred tools** (Plan и подобные) — каждая загрузка схемы стоит токенов.

```

- [x] **Step 3: Удалить упоминание `(траектория)` из «Формат самопроверок» (#6)**

В разделе «Формат самопроверок» (около строки 27 в текущей версии — первый маркированный список). Заменить блок:

```markdown
У тебя четыре самопроверки в пайплайне — все одного формата:

- **после сегментации** (заменяет старые №1 и №2),
- **после Логической реконструкции**,
- **после Траектории**,
- **после сборки мастер-MD** (заменяет старую №4).
```

на:

```markdown
У тебя три самопроверки в пайплайне — все одного формата:

- **после сегментации**,
- **после Логической реконструкции**,
- **после сборки мастер-MD**.
```

И в финале того же раздела (строка ~73 — «Эти четыре самопроверки — служебные»):

```markdown
Эти четыре самопроверки — служебные, перед сохранением мастер-MD
вырезаются (см. `### Очистка перед сохранением`).
```

заменить на:

```markdown
Эти три самопроверки — служебные, перед сохранением мастер-MD
вырезаются (см. `### Очистка перед сохранением`).
```

- [x] **Step 4: Дополнить методичку сегментации в «Глобальный обзор — методолог» (#7)**

В разделе «Глобальный обзор — методолог», блок «Что такое сегмент» (около строк 92–101). Заменить текущий блок:

```markdown
### Что такое сегмент

> Сегмент = одна самостоятельная мысль автора, ради которой он отвёл часть
> времени. Если фрагмент не несёт самостоятельной мысли (переход, разогрев,
> связка, обрамление) — это не сегмент.

Если ты не можешь сформулировать мысль сегмента одной фразой — скорее всего,
это либо два разных сегмента слиплись, либо фрагмент служебный.

Размер сегмента **может быть любым**. Один сегмент может длиться 30 минут,
другой — 3 минуты. Главное — самостоятельность мысли. Балансировать сегменты
по объёму ради красоты — не нужно.
```

на:

```markdown
### Что такое сегмент

> Сегмент = одна **крупная самостоятельная мысль автора**, ради которой он отвёл существенный кусок времени, со всеми её разворотами — концепт, демо, минусы, обход, ещё демо. Уровень «**главы курса**», а не «параграфа».

Если ты не можешь сформулировать мысль сегмента одной фразой — скорее всего, это либо два разных сегмента слиплись, либо фрагмент служебный.

**Целевой масштаб:** ориентир — **5–10 сегментов на 60–90 минут**, ~10–15 минут на сегмент. Если получилось заметно больше (15+ сегментов на полтора часа) — почти всегда это размельчение.

**Использование `[SERIAL CONTEXT]` как масштабного эталона:** если есть мастер-MD предыдущей части серии — посмотри, сколько сегментов было там при сопоставимой длительности, и держи аналогичный масштаб. Это сильнейший ориентир для масштаба.
```

- [x] **Step 5: Добавить «Анти-маркеры» и «Тест на укрупнение» в «Глобальный обзор — методолог» (#7)**

В том же разделе после «Маркеры потенциальной границы» (после блока про логическую связность, около строки 124 — после абзаца «Если по одну сторону служебный фрагмент — он растворяется в соседнем сегменте.»). Добавить:

```markdown

### Анти-маркеры

Что **не** является границей сегмента:

- Смена формата внутри одной темы (теория → демо → ещё демо одной темы) — развороты одной мысли.
- Новая команда / фича / лайфхак, раскрывающая ту же мысль — расширение примера, не новый сегмент.
- Метафора, образ, оговорка — часть стиля.

Признак анти-маркера: формат меняется, но **главная мысль фрагмента остаётся той же**.

### Тест на укрупнение

Перед выдачей чистовой карты пройдись по соседним парам сегментов: «если объединить эти два, получится одна развёрнутая мысль, формулируемая одной фразой?» Если да — объединить. Если нет — оставить раздельно.
```

- [x] **Step 6: Усилить «Самопроверку — глазами Аудитории (сегментация)» (#7)**

В том же разделе, в подразделе «**На что обратить внимание при чтении**» (около строки 161). К текущему списку добавить новый пункт первым:

```markdown
- **Не слишком ли я дроблю?** Не дал ли я каждой новой команде, демо или лайфхаку отдельный сегмент вместо того, чтобы держать их как части одной крупной мысли? Если в карте >10 сегментов на 60–90 минут — это сигнал перечитать критически и применить тест на укрупнение.
```

- [x] **Step 7: Удалить шаги проверки читательского блока субагента в «Верификация текста» (#1)**

В разделе «Верификация текста» удалить весь подблок про читательский блок (строки 199–209 в текущей версии). Это блок, начинающийся с:

```markdown
**Читательский блок `### Самопроверка — глазами Аудитории`:**

- **Блок отсутствует** → запрос на переделку: ...
- **Блок есть, но рефлексия в третьем лице или без ссылок на конкретные
  места** → запрос на переделку: ...
- **Блок есть с перечнем правок, но в Тексте эти правки не применены**
  (выборочная сверка по 1–2 пунктам перечня) → запрос на переделку: ...
```

Целиком удаляется. После удаления раздел «Шаг 1: Чтение блоков самопроверки субагента» завершается на словах:

```markdown
Если технический блок в порядке — перейти к шагу 2.
```

(заменить «оба блока» на «технический блок», если такая фраза есть в финале шага 1.)

- [x] **Step 8: Добавить раздел «Подготовка к Фазе 2» — встраивание спек, Sonnet, гибридный ToV (#2, #3, #4)**

В brigadir.md есть упоминания подготовки Фазы 2 (через `slice_chunk`), но нет явного раздела про сборку промтов. Добавить новый раздел сразу **перед** разделом «Верификация текста» (то есть после «Глобальный обзор — методолог» и связанной самопроверки, около строки 180). Содержание раздела:

```markdown

---

## Подготовка к Фазе 2 (между сегментацией и запуском субагентов)

### 1. Гибридный ToV

ToV (Tone of Voice) — динамический блок с характерными чертами речи спикера. Логика:

1. Имя серии определяется из имени транскрипта (например, `ПОЛНЫЙ курс Claude Code 2026 ч2.txt` → имя серии `full-claude-code-2026`, файл ToV — `transcripts/tov_full-claude-code-2026.md`).
2. Проверить через `Read` или `Glob`, существует ли файл `transcripts/tov_<серия>.md`.
3. **Если файл есть** — прочитать его (~1K токенов), использовать содержимое как блок `[TOV_DYNAMIC]`.
4. **Если файла нет** — запустить:
   ```
   python .claude/skills/konspekt/preprocessor.py head <транскрипт>
   ```
   Получить первые ~2500 токенов транскрипта без таймштампов. Извлечь:
   - Характерные обороты речи
   - Темп и формальность
   - Фирменные фразы и обращения к аудитории
   
   Записать как блок `[TOV_DYNAMIC]...[/TOV_DYNAMIC]` (~150–200 токенов). **Сохранить** его в файл `transcripts/tov_<серия>.md` для будущих частей серии.

### 2. Чтение спек один раз

Прочитать через `Read`:
- `.claude/skills/konspekt/subagent_phase2.md` → переменная `[INSTRUCTIONS]`
- `.claude/skills/konspekt/tov_spec.md` → переменная `[TOV_BASE]`
- `.claude/skills/konspekt/audience_spec.md` → переменная `[AUDIENCE]`

Эти файлы передаются каждому субагенту Ф2 **в промте**, а не через Read внутри субагента. Это экономит ~9K × N токенов на повторное чтение неизменного контента.

### 3. Нарезка чанков по сегментам

Для каждого сегмента из чистовой карты выполнить:

```
python .claude/skills/konspekt/preprocessor.py slice <чанк-файл> --from HH:MM:SS --to HH:MM:SS
```

Получить `[CHUNK_сегмента]` — текст только этого сегмента.

### 4. Запуск субагентов Ф2 на Sonnet, параллельно

Для каждого сегмента запустить Agent-вызов с явным указанием модели:

- **Параметр `model: "sonnet"`** в каждом Agent-вызове. Бригадир остаётся на Opus, субагенты Ф2 — на Sonnet (экономия ~×5).
- Параметр `prompt` содержит блоки:
  ```
  [INSTRUCTIONS]
  <содержимое subagent_phase2.md>
  
  [TOV SPEC]
  <содержимое tov_spec.md>
  [TOV_DYNAMIC]
  <динамический блок ToV>
  [/TOV_DYNAMIC]
  
  [AUDIENCE]
  <содержимое audience_spec.md>
  
  [NAV MAP]
  <чистовая карта целиком>
  
  [SEGMENT SPEC]
  <запись твоего сегмента из чистовой карты>
  
  [CHUNK_сегмента]
  <вывод preprocessor.py slice>
  ```

Все субагенты Ф2 запускаются параллельно — одним сообщением с несколькими Agent-вызовами.
```

- [x] **Step 9: Добавить указание `model: "sonnet"` для Фазы 1 (#3)**

В brigadir.md явного раздела про запуск Фазы 1 нет (он в `SKILL.md`). Но в начале «Глобальный обзор — методолог» есть упоминание входных данных. Перед разделом «Глобальный обзор» — то есть в верхней части файла, после «Дисциплина бригадира», но до «Формат самопроверок» — добавить короткий раздел:

```markdown

---

## Запуск Фазы 1

Когда `SKILL.md` диктует запуск Фазы 1 — субагенты сегментации запускаются параллельно через Agent tool. **Каждый Agent-вызов указывает `model: "sonnet"`** — субагенты Ф1 работают на Sonnet 4.6 (бригадир остаётся на Opus).

В промт каждого субагента Ф1 встраивается:
- Содержимое `subagent_phase1.md` (бригадир читает один раз через Read и передаёт всем)
- Содержимое `audience_spec.md` (тоже один раз → всем)
- `[CHUNK]`, `[SERIAL CONTEXT]`, `[PROFILE]` — по специфике вызова
```

- [x] **Step 10: Заморозить раздел «Траектория — методолог» (#6)**

Раздел «Траектория — методолог» (текущие строки 337–391, всё от заголовка `## Траектория — методолог` до начала следующего `## Сборка мастер-MD — редактор`). Обернуть весь блок HTML-комментарием:

В начало раздела (прямо после `---`, перед `## Траектория — методолог`) вставить строку:

```markdown
<!-- FROZEN: раздел заморожен. Не выполняется пайплайном. Сохранён для будущих ресурсов на доработку. См. spec docs/superpowers/specs/2026-05-17-konspekt-token-optimization.md пункт #6. Раскомментировать (удалить FROZEN-обёртку) при возврате к работе над раскрытием траектории.
```

И в конце раздела (после блока самопроверки траектории, перед следующим `---`) — закрывающую строку:

```markdown
-->
```

То есть весь блок «## Траектория — методолог» оказывается внутри HTML-комментария и не воздействует на пайплайн.

- [x] **Step 11: Удалить `(траектория)` из «Очистка перед сохранением» (#6)**

В разделе «Очистка перед сохранением» (около строки 452). Заменить блок:

```markdown
- Твои собственные блоки `### Самопроверка — глазами Аудитории (*)` —
  все четыре: `(сегментация)`, `(реконструкция)`, `(траектория)`, `(мастер-MD)`.
```

на:

```markdown
- Твои собственные блоки `### Самопроверка — глазами Аудитории (*)` —
  все три: `(сегментация)`, `(реконструкция)`, `(мастер-MD)`.
```

- [x] **Step 12: Обновить шаблон сегмента в «Сборка мастер-MD — редактор» (#8 + #6)**

В разделе «Сборка мастер-MD — редактор» (текущие строки 395–448) в шаблоне мастер-MD:

- Найти блок шаблона сегмента (около строк 425–443). Сейчас:
  ```markdown
  ## Сегмент 1 | HH:MM:SS–HH:MM:SS | [Тема]
  
  **Тип:** [риторическая роль]
  **Спикер:** [имя или «основной»]
  
  **Ключевая мысль:** [1–2 предложения]
  
  ### Карта
  
  - [буллет]
  
  ### Текст
  
  [текст]
  ```
  
  Заменить на:
  ```markdown
  ## Сегмент 1 | HH:MM:SS–HH:MM:SS | [Тема]
  
  **Тип:** [риторическая роль]
  **Спикер:** [имя или «основной»]
  
  **Ключевая мысль:** [1–2 предложения]
  
  ### Карта
  
  - **(И)** [тезис идейного уровня]
  - **(М)** [тезис методологического уровня]
  - **(Д)** [тезис операционального уровня]
  
  ### Текст
  
  [Текст. Если в сегменте несколько подтем — членение `####` подразделами.
  Внутри Текста при явных формулировках спикера могут стоять блоки:
  > **Ключевая идея:** / > **Принцип:** / > **Методология:** /
  > **Лайфхак:** / > **Демонстрация:** / > **Важно:**]
  ```

- Убедиться, что в шаблоне мастер-MD **отсутствует** раздел `## Траектория` (он должен быть удалён при заморозке). Если есть упоминание `## Траектория` в шаблоне — удалить.

- [x] **Step 13: Усилить «Финальную самопроверку мастер-MD» (#1)**

В разделе «Финальная самопроверка мастер-MD — глазами Аудитории» (около строки 480) в блоке «Ориентиры для рефлексии при чтении всего мастер-MD» — к существующему списку **добавить два новых пункта** в начало:

```markdown
- **Понятна ли мне как Аудитории логика автора через каждый Текст?** Не запутался ли я в конкретных формулировках субагентов? Не звучит ли где-то нейтральный учебный стиль вместо голоса спикера? (Эта проверка раньше была у субагентов в виде читательской самопроверки; теперь — на тебе как на единственном «целостном читателе».)
- **Складываются ли уровни (И)/(М)/(Д) в Картах в осмысленную картину?** Видны ли в материале идеи (стратегия), методологии (тактика), действия (операционал)? Не перекошен ли материал в одну сторону?
```

- [x] **Step 14: Финальная сверка через Grep**

Проверить:
- `FROZEN` встречается (заморозка Траектории)
- `model: "sonnet"` встречается **дважды** (для Ф1 и Ф2)
- `tov_<серия>.md` упомянуто (гибридный ToV)
- `preprocessor.py head` упомянуто
- `**(И)**`, `**(М)**`, `**(Д)**` упомянуты в шаблоне сегмента
- `Дисциплина` (как раздел) упомянута
- НЕТ упоминания «Траектория — методолог» как живого этапа (только внутри FROZEN-комментария)
- НЕТ упоминания читательской самопроверки субагента Ф2

- [x] **Step 15: Коммит**

```
git add .claude/skills/konspekt/brigadir.md
git commit -m "feat(konspekt): brigadir.md — дисциплина, Sonnet, гибридный ToV, заморозка Траектории, методичка сегментации, шаблон с (И)/(М)/(Д)"
```

---

## Task 6: SKILL.md — обзор пайплайна (#3, #4, #5, #6)

**Files:**
- Modify: `.claude/skills/konspekt/SKILL.md`

`SKILL.md` — обзорный пайплайн. Его правки опираются на уже изменённый `brigadir.md`, поэтому делаем последним среди файлов скилла.

- [ ] **Step 1: Прочитать текущий SKILL.md**

Уже сделано на этапе планирования.

- [ ] **Step 2: Обновить описание пайплайна Фазы 1 — указать Sonnet (#3)**

В SKILL.md, раздел «Фаза 1: Черновая сегментация (параллельно)» (около строк 118–131). Заменить текущий первый абзац:

```markdown
Запустить по одному субагенту на каждый чанк, параллельно.

Прочитать `subagent_phase1.md` и передать его содержимое каждому субагенту в начале промпта.
```

на:

```markdown
Запустить по одному субагенту на каждый чанк, **параллельно**. **Модель каждого субагента — Sonnet** (бригадир остаётся на Opus). В Agent-вызове указать `model: "sonnet"`.

Прочитать `subagent_phase1.md` и `audience_spec.md` (один раз через Read) и встроить их содержимое в промт каждого субагента — субагенты сами Read к этим файлам не делают.
```

- [ ] **Step 3: Обновить описание пайплайна Фазы 2 — указать Sonnet и встраивание спек (#2 + #3)**

В SKILL.md, раздел «Фаза 2: Написание текста (параллельно)» (около строк 146–166). Заменить текущий блок:

```markdown
Запустить по одному субагенту на каждый сегмент из чистовой карты, параллельно.

Прочитать `subagent_phase2.md` и передать его содержимое каждому субагенту в начале промпта.

Каждый субагент получает:
- `[NAV MAP]` — полная чистовая карта
- `[TOV SPEC]` — содержимое `tov_spec.md` + динамический блок ToV
- `[SEGMENT SPEC]` — запись своего сегмента из чистовой карты
- `[CHUNK_сегмента]` — отрезок транскрипта по границам сегмента (вывод `preprocessor.py slice`)

Результат: `### Карта` + `### Текст` для каждого сегмента.
```

на:

```markdown
Перед запуском Ф2 бригадир выполняет **подготовку промтов** (см. brigadir.md раздел «Подготовка к Фазе 2»): один раз читает спеки `subagent_phase2.md`, `tov_spec.md`, `audience_spec.md`; обеспечивает блок ToV через гибридную логику (файл серии или head-выборка); нарезает чанки по сегментам через `preprocessor.py slice`.

Запустить по одному субагенту на каждый сегмент из чистовой карты, **параллельно**. **Модель каждого субагента — Sonnet** (бригадир остаётся на Opus). В Agent-вызове указать `model: "sonnet"`.

Каждый субагент получает в промте (встраивается, не через Read):
- `[INSTRUCTIONS]` — содержимое `subagent_phase2.md`
- `[TOV SPEC]` — содержимое `tov_spec.md` + динамический блок `[TOV_DYNAMIC]`
- `[AUDIENCE]` — содержимое `audience_spec.md`
- `[NAV MAP]` — полная чистовая карта
- `[SEGMENT SPEC]` — запись своего сегмента
- `[CHUNK_сегмента]` — вывод `preprocessor.py slice`

Результат: `**Ключевая мысль:**` + `### Карта` (с метками (И)/(М)/(Д)) + `### Текст` (с `####` подразделами при наличии подтем) + `### Самопроверка` (техническая) для каждого сегмента.
```

- [ ] **Step 4: Обновить раздел «Динамический ToV» с гибридной логикой (#4)**

В SKILL.md, раздел «Динамический ToV» (строки 112–116). Заменить текущий блок:

```markdown
## Динамический ToV

Прочитать первые ~2–3K токенов транскрипта. Выделить характерные обороты спикера, темп, фирменные фразы. Записать как `[TOV_DYNAMIC]...[/TOV_DYNAMIC]` блок (~150–200 токенов).
```

на:

```markdown
## Динамический ToV (гибридная стратегия)

Перед Фазой 2 бригадир обеспечивает блок `[TOV_DYNAMIC]`. Логика — гибридная:

1. Имя серии определяется из имени транскрипта.
2. Если существует файл `transcripts/tov_<серия>.md` — взять его содержимое (~1K токенов).
3. Если файла нет — запустить `python .claude/skills/konspekt/preprocessor.py head <транскрипт>`, получить первые ~2500 токенов транскрипта без таймштампов; извлечь характерные обороты, темп, фирменные фразы; записать как блок и **сохранить** его в файл `transcripts/tov_<серия>.md` для следующих частей серии.

Подробности — в `brigadir.md` разделе «Подготовка к Фазе 2».
```

- [ ] **Step 5: Обновить описание сборки — убрать «Траекторию» (#6)**

В SKILL.md, раздел «Бригадир: Сборка» (около строки 170–174). Заменить текущий блок:

```markdown
## Бригадир: Сборка

Прочитать `brigadir.md` (и `audience_spec.md`, если ещё не загружен). Выполнить разделы по порядку: «Верификация текста», «Гармонизация стиля», «Переходы между сегментами», «Логическая реконструкция — методолог» (с самопроверкой), «Траектория — методолог» (с самопроверкой), «Сборка мастер-MD — редактор», «Финальная самопроверка мастер-MD — глазами Аудитории». После каждой самопроверки бригадира — применить найденные правки до перехода к следующему шагу.
```

на:

```markdown
## Бригадир: Сборка

Выполнить разделы `brigadir.md` по порядку: «Верификация текста», «Гармонизация стиля», «Переходы между сегментами», «Логическая реконструкция — методолог» (с самопроверкой), «Сборка мастер-MD — редактор», «Финальная самопроверка мастер-MD — глазами Аудитории». После каждой самопроверки бригадира — применить найденные правки до перехода к следующему шагу.

Раздел «Траектория» **заморожен** (см. brigadir.md, FROZEN-комментарий) и не выполняется.
```

- [ ] **Step 6: Добавить раздел «Дисциплина бригадира» после «Точки остановки» (#5)**

В SKILL.md, в самом конце файла (после раздела «Точки остановки», после таблицы строк 194–200). Добавить:

```markdown

---

## Дисциплина бригадира

Эти правила сокращают накладные расходы бригадира на контекст. Подробнее — в `brigadir.md` раздел «Дисциплина».

1. **В /konspekt не использовать TodoWrite** — пайплайн линейный, прогресс виден из текста.
2. **Не перечитывать чанки после Ф1.** Точечные Read с конкретным таймштампом — только при разрешении сомнений в сегментации.
3. **Не запускать дополнительные deferred tools** (Plan и подобные).
```

- [ ] **Step 7: Финальная сверка**

Через Grep убедиться:
- `model: "sonnet"` упомянуто (минимум 2 раза — для Ф1 и Ф2)
- `preprocessor.py head` упомянуто
- `tov_<серия>.md` упомянуто
- `(И)`, `(М)`, `(Д)` или хотя бы упоминание уровней
- `Дисциплина` (как раздел)
- НЕТ упоминания «Траектория — методолог» как живой части пайплайна (только в контексте «заморожен»)

- [ ] **Step 8: Коммит**

```
git add .claude/skills/konspekt/SKILL.md
git commit -m "feat(konspekt): SKILL.md — Sonnet для Ф1/Ф2, гибридный ToV, дисциплина, удаление Траектории"
```

---

## Task 7: Локальная сверка и handoff на тестовый прогон

**Files:** —

Эта задача проверяет согласованность всех правок между файлами **локально, без запуска скилла**, и готовит handoff-сообщение для следующей сессии, в которой будет проведён тестовый прогон.

- [ ] **Step 1: Проверить кросс-ссылки между файлами**

Через Grep по всему `.claude/skills/konspekt/`:
- `model: "sonnet"` — должно встречаться в `brigadir.md` (мин 2 раза: Ф1 и Ф2) и `SKILL.md` (мин 2 раза)
- `preprocessor.py head` — в `brigadir.md` и `SKILL.md`
- `tov_<серия>.md` — в `brigadir.md` и `SKILL.md`
- `[TOV_DYNAMIC]` — в `tov_spec.md`, `brigadir.md`, `SKILL.md`
- `(И)`, `(М)`, `(Д)` — в `subagent_phase2.md`, `tov_spec.md`, `brigadir.md`

Если что-то не сходится — найти и поправить.

- [ ] **Step 2: Проверить, что Траектория действительно заморожена**

Через Grep:
```
Grep pattern="## Траектория" path=".claude/skills/konspekt/"
```

Ожидаемое: единственное упоминание `## Траектория — методолог` находится **внутри** HTML-комментария `<!-- FROZEN: ... -->` в `brigadir.md`. Никаких упоминаний раздела `## Траектория` как живой части пайплайна в `SKILL.md`. Никаких упоминаний раздела в шаблоне мастер-MD.

- [ ] **Step 3: Проверить, что читательская самопроверка субагента удалена**

Через Grep по `subagent_phase2.md`:
```
Grep pattern="Самопроверка — глазами Аудитории" path=".claude/skills/konspekt/subagent_phase2.md"
```

Ожидаемое: 0 совпадений (читательская самопроверка удалена из субагента Ф2). Если есть — удалить.

В `brigadir.md` блок «Самопроверка — глазами Аудитории (сегментация)», «...(реконструкция)», «...(мастер-MD)» должны остаться. Через Grep:
```
Grep pattern="Самопроверка — глазами Аудитории" path=".claude/skills/konspekt/brigadir.md"
```

Ожидаемое: ~3–5 совпадений (заголовки трёх самопроверок и упоминания в формате).

- [ ] **Step 4: Прогнать preprocessor.py head на эталонном транскрипте**

Эталонный транскрипт — `transcripts/Урок 2.srt` (45 минут). Команда:

```
python .claude/skills/konspekt/preprocessor.py head "transcripts/Урок 2.srt"
```

Ожидаемое: на stdout первые ~2500 токенов транскрипта без `[HH:MM]` разметки. Команда выходит без ошибок. Если возникает ошибка парсинга SRT — это может быть отдельный вопрос (SRT — не TXT с таймштампами в формате `[HH:MM]`); зафиксировать как риск для handoff (см. шаг 6).

- [ ] **Step 5: Проверить размеры файлов**

Команда:

```
wc -l .claude/skills/konspekt/*.md
```

Ожидаемое:
- `subagent_phase2.md` — ~150–180 строк (было 289)
- `tov_spec.md` — ~70–80 строк (было 138)
- `subagent_phase1.md` — немного увеличился (добавлены анти-маркеры)
- `brigadir.md` — увеличился (добавлены разделы Дисциплина, Подготовка к Ф2; уменьшился за счёт удаления читательской проверки и заморозки Траектории — но Траектория осталась внутри FROZEN, так что нетто-рост)
- `SKILL.md` — слегка увеличился (Дисциплина, гибридный ToV)

- [ ] **Step 6: Подготовить handoff-сообщение для тестового прогона**

Создать заметку для следующей сессии (можно через Write в файле `docs/konspekt-optimization-test-handoff.md` или через коммит-сообщение). Содержание заметки:

```markdown
# Handoff: тестовый прогон оптимизированного /konspekt

**Цель сессии:** провести единый огневой прогон правок плана
`docs/superpowers/plans/2026-05-17-konspekt-token-optimization.md`
на эталонном транскрипте.

**Эталон:** `transcripts/Урок 2.srt` (~45 минут).

**Процедура:**
1. Запустить `/konspekt` на эталоне.
2. Проверить чек-лист (см. ниже).
3. Зафиксировать токенный расход прогона.

**Чек-лист самопроверки результата:**
- Авторский голос сохранён (выборочно: 3 случайных абзаца).
- Объём Текста на сегмент 70–90% от транскрипта.
- Сегментация: **3–5 сегментов** на 45 минут (не 8+).
- Подзаголовки `####` внутри Текста при наличии нескольких подтем.
- В Карте каждого сегмента — метки **(И)/(М)/(Д)** на буллетах.
- Новые блоки `> Ключевая идея` / `> Принцип` / `> Методология`
  появляются при явных формулировках спикера.
- Финальная самопроверка бригадира включает читательскую оптику
  по всему мастер-MD.
- В мастер-MD НЕТ раздела `## Траектория`.
- В мастер-MD НЕТ блоков `### Самопроверка — глазами Аудитории` от субагентов.

**Ожидаемая токенная отметка для 45 мин:** ~125–150K.

**Известный риск:** transcripts/Урок 2.srt — это .srt файл. Если
preprocessor.py не парсит SRT-формат (он рассчитан на текст с разметкой
[HH:MM]), может потребоваться предварительная конвертация SRT → TXT
или доработка parse_transcript_lines в preprocessor.py.

**Если качество просело:**
- Откатить #3 (модели Sonnet) до F1 (Sonnet только для Ф2) или
  до F4 (всё на Opus), сохранить остальные правки. Перепрогон.
```

- [ ] **Step 7: Финальный коммит handoff-заметки (опционально, если шаг 6 сделан в файле)**

Если handoff-заметка записана в файл:

```
git add docs/konspekt-optimization-test-handoff.md
git commit -m "docs(konspekt): handoff для тестового прогона оптимизаций"
```

Если заметка передана пользователю текстом — коммит не нужен.

- [ ] **Step 8: Сообщение пользователю о завершении**

После всех коммитов отчитаться пользователю:

> Все 10 пунктов спеки реализованы и закоммичены. Файлы скилла /konspekt
> обновлены: preprocessor.py (новая команда head), subagent_phase1.md,
> subagent_phase2.md, tov_spec.md, brigadir.md, SKILL.md.
>
> Тестовый прогон скилла на эталонном transcripts/Урок 2.srt — следующий
> шаг, выполняется **в отдельной сессии** (требование handoff-документа,
> чтобы не съесть rate-limit в той же сессии, где делались правки).
>
> Handoff-заметка с чек-листом — в [файл/тексте].

---

## Self-Review Checklist

После завершения всех Tasks свериться:

**1. Spec coverage:**

| Пункт спеки | Где реализован |
|---|---|
| #1 | Task 3 step 2 (удаление из subagent_phase2), Task 5 step 7 (удаление проверки в brigadir), Task 5 step 13 (усиление финальной самопроверки) |
| #2 | Task 3 step 2 (сокращение subagent_phase2), Task 4 step 2 (сокращение tov_spec), Task 5 step 8 (встраивание спек) |
| #3 | Task 5 step 8 (model: "sonnet" для Ф2), Task 5 step 9 (для Ф1), Task 6 steps 2–3 (SKILL.md) |
| #4 | Task 1 (preprocessor.py head), Task 5 step 8 (логика гибридного ToV), Task 6 step 4 (SKILL.md) |
| #5 | Task 5 step 2 (раздел Дисциплина в brigadir), Task 6 step 6 (раздел в SKILL.md) |
| #6 | Task 5 step 3 (удаление из формата самопроверок), Task 5 step 10 (FROZEN-обёртка), Task 5 step 11 (очистка), Task 5 step 12 (шаблон без Траектории), Task 6 step 5 (SKILL.md) |
| #7 | Task 2 (методичка в subagent_phase1), Task 5 steps 4–6 (методичка в brigadir + усиление самопроверки) |
| #8 | Task 3 step 2 (правила `####` в subagent_phase2), Task 5 step 12 (шаблон сегмента) |
| #9 | Task 3 step 2 (блоки Ключевая идея, Принцип) |
| #10 | Task 3 step 2 (блок Методология + маркировка (И)/(М)/(Д)) |

Все 10 пунктов покрыты задачами.

**2. Placeholder scan:** все шаги содержат точные Markdown-блоки для вставки/замены или точные команды. Нет «TBD», «реализовать аналогично», «по образцу».

**3. Type consistency:** имена встроенных блоков консистентны во всём плане:
- `[INSTRUCTIONS]`, `[TOV SPEC]` (или `[TOV_BASE]` — выбрана единая форма `[TOV SPEC]` в финальном промте субагента), `[AUDIENCE]`, `[NAV MAP]`, `[SEGMENT SPEC]`, `[CHUNK_сегмента]`
- Метки уровней: `(И)`, `(М)`, `(Д)` — везде одинаково
- Имена файлов: `subagent_phase2.md`, `tov_spec.md`, `audience_spec.md`, `brigadir.md`, `SKILL.md`, `preprocessor.py`, `tov_<серия>.md` — везде одинаково
- Команда: `preprocessor.py head <файл> [--tokens N]` — везде одинаково

Если при выполнении плана обнаружится несоответствие имён — единая форма устанавливается по `subagent_phase2.md` (он передаётся как `[INSTRUCTIONS]`, а спека стиля — как `[TOV SPEC]`).

**4. Ambiguity check:** все спорные интерпретации зафиксированы:
- При сомнении в уровне маркировки — выбирать `(Д)` (правило указано в Task 3 step 2).
- При сомнении, применять ли специальный блок — оставлять в обычном тексте (правило в Task 3 step 2).
- Имя серии для файла `tov_<серия>.md` — определяется из имени транскрипта (правило в Task 5 step 8).
