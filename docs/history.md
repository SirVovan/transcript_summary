# История развития скилла /konspekt

> Хронология архитектурных решений: что меняли, почему именно так, какие альтернативы рассматривали.

---

## Предыстория: трёхшаговая архитектура

До мая 2026 скилл работал по строго последовательной схеме:

Транскрипт → ШАГ 1: Карта смыслов (XLSX + HTML) → ШАГ 2: Конспект (DOCX) → ШАГ 3: Виджет (HTML)

Каждый шаг — отдельный вызов Claude. Каждый шаг загружал свои инструкции
(kartograf.md — 26.5 KB, konspektolog.md — 14.1 KB, konstitutsiya.md — 10.5 KB,
xlsx_template.py — 14.7 KB).

**Фундаментальные проблемы:**
- Транскрипт читался дважды — на карту и на конспект. При типичном 3–4-часовом
  видео это ~36–48K токенов × 2.
- Вычисление смешано с представлением: картограф строил структуру И думал про
  XLSX-шаблоны; конспектолог писал текст И думал про DOCX. Инструкции раздуты.
- Пользователь вручную резал длинные транскрипты на части.

---

## 2026-05-04 — Слой 1: разделение вычисления и представления

**Спек:** 209 строк.

### Что хотели исправить

Двойное чтение транскрипта, смешение логики и форматов, ручная нарезка.

### Ключевое решение: два слоя

СЛОЙ 1: Вычисление → производит мастер-MD (структура + текст)
СЛОЙ 2: Представление → рендерит мастер-MD в форматы (отложен)

Слой 1 — единственный артефакт: `мастер.md`. Содержит и сегменты, и карту,
и текст. Слой 2 — механическая конвертация, без редакции.

### Пайплайн Слоя 1 (первая версия)

ШАГ 0: Предобработка (>25K → нарезка скриптом)
  ↓
ШАГ 1а: Сегментация + самопроверка по 4 критериям
  ↓
ШАГ 1б: Параллельные субагенты — по одному на сегмент (Карта + Текст)
  ↓
ШАГ 1в: Бригадир (верификация, гармонизация, переходы, сборка)
  ↓
Мастер-MD

### Почему порог 25K токенов

Эмпирически: ~2 часа видео. До этого Claude справляется в одном контексте.
Выше — нужна нарезка.

### Как работает нарезка (ШАГ 0)

Гибридный подход: скрипт + LLM.
1. preprocessor.py накапливает токены, при ~15–18K вырезает окно ±10 минут.
2. Лёгкий LLM-вызов ищет смысловую границу в окне.
3. Уверен → режет молча. Сомневается → пауза, запрос пользователю.
4. Чанки по ~15–18K токенов, перекрытие ~500 токенов на стыке.

Альтернатива (тупая нарезка по размеру) отвергнута: резать посередине мысли —
получить рваный контент. Смысловая граница важнее равного размера.

### Почему субагенты, а не один Claude

Один Claude на 36K транскрипта + инструкции + текст → контекст ~60K,
качество падает к концу. Субагент получает только свой сегмент (~2–5K) +
навигационную карту (~1–2K) + ToV (~200 токенов). Контекст маленький,
качество стабильное.

### ToV-спецификация (голос спикера) — гибридная

- Базовая часть (фиксированная): первое лицо, живая речь, без академизма.
- Динамическая часть: Claude читает первые ~2–3K токенов, выделяет обороты,
  темп, фирменные фразы конкретного спикера (~150–200 токенов).
- Зачем: субагенты пишут в голосе этого спикера, а не абстрактно.

### Роль бригадира

Верифицирует (все ли тезисы карты в тексте), гармонизирует стиль, пишет
переходы. Проблема → переделка субагента. Второй сбой → метка ⚠️, продолжение.

### Что явно убрали

XLSX, DOCX, карта-MD как отдельный артефакт. Двойное чтение транскрипта.

### Реализовано

preprocessor.py, tov_spec.md, segmentator.md, subagent_writer.md,
brigadir.md, SKILL.md (перезапись). Старые файлы → _archived_*.

---

## 2026-05-05 (утро) — Двухфазный пайплайн: уроки реального прогона

**Спек:** 288 строк. Заменяет спек от 2026-05-04.

### Что вскрылось на реальном транскрипте

**Проблема 1 — Неравномерная нагрузка.** Субагент на сегмент: сегменты
от 5 до 40 минут. Часть субагентов простаивает, часть тонет. Качество
нестабильно.

**Проблема 2 — Некачественная сегментация.** Q&A-блоки не выделялись
в отдельные сегменты. Сегмент 4 помечен «лекция-практика + Q&A» —
должен быть разбит на два.

### Решение: субагент на чанк, не на сегмент

Чанки ~15–18K токенов, примерно одинакового размера. Нагрузка предсказуема.

Альтернатива (группировка сегментов в пакеты одинакового размера) отвергнута:
требует сначала отсегментировать весь транскрипт — а сегментация и была
проблемой. Чанки режутся до сегментации, по размеру, без знания структуры.

### Решение: двухфазный пайплайн

**Фаза 1 — Черновая сегментация (параллельно по чанкам):**
Субагент получает чанк → разбивает на сегменты с метаданными. Текст не пишет.

**Бригадир — Глобальный обзор (между фазами):**
Получает черновые карты всех чанков → чистовая карта всего выступления.
Проверяет по трём критериям:
1. Единство риторической функции — смешанный тег = сигнал к разбивке.
2. Сигналы смены режима автора — «отвечаю на вопрос», «сделаем паузу» и т.д.
3. Логическая связность — один сегмент = ответ на один вопрос «почему здесь?».

При неопределённости — точечное чтение фрагмента транскрипта по таймштампу.

**Фаза 2 — Написание текста (параллельно по сегментам чистовой карты):**
Субагент получает чистовую карту + ToV + свой сегмент + кусок транскрипта.
Пишет Карту + Текст.

### Почему бригадир между фазами, а не после

Если бы бригадир работал после Фазы 2 — при изменении сегментации пришлось бы
переписывать уже готовый текст. Глобальный обзор фиксирует структуру до того,
как субагенты вложат время в текст.

### Лёгкий режим (≤ 25K)

Короткие транскрипты: Claude сам делает всё без субагентов — накладные расходы
не оправданы. Те же критерии, тот же формат.

### Логическая реконструкция

Новый раздел перед сегментами: абзац прозой (аргументация выступления) +
таблица (сегмент → роль → ход автора). Один сегмент — только абзац.

### Что изменилось

| Было | Стало |
|---|---|
| Субагент на сегмент | Субагент на чанк |
| Один проход | Два прохода (сегментация → обзор → текст) |
| Сегментатор — один агент | Черновая сегментация — параллельные субагенты |
| Бригадир только собирает | Бригадир: качество сегментации + реконструкция |
| Нет реконструкции | Раздел «Логическая реконструкция» |

### Реализовано

segmentator.md → _archived_, subagent_writer.md → _archived_.
Созданы subagent_phase1.md, subagent_phase2.md.
brigadir.md и SKILL.md переписаны.

---

## 2026-05-05 (вечер) — Слой 2: виджет + форматирование

**Спеки:** 277 строк (виджет) + 148 строк (форматирование).

### Слой 2: виджет из мастер-MD

**Принцип:** механическая конвертация, не редакция. Контент без потерь.

**Почему JSON-промежуточный слой:** Claude не генерит HTML напрямую (опасно,
раздувает контекст). Пишет JSON (~2–3K токенов) → widget_generator.py
собирает HTML (~15–20K). Контекст Claude маленький.

**Структура:**
- Левая колонка = Текст, правая = Карта (буллеты как insight-карточки)
- Один экран = один сегмент
- Реконструкция — первый экран (сегмент '00')
- Спецэлементы: pr-block (промпт+копирование), cycle (диаграмма шагов)

**Цвета по риторической роли:**
- Открытие/введение → синий (concept)
- Инструктаж/метод → зелёный (method)
- Демонстрация/практика → оранжевый (demo)

### Форматирование мастер-MD

**4 проблемы после первого прогона:**
1. Текст — сплошной нарратив без выделений.
2. Лайфхаки/советы/демонстрации не выделены.
3. Карта богаче Текста (буллеты не раскрыты).
4. Нет ключевой мысли перед сегментом.

**Решения:**
- **Ключевая мысль** — 1–2 предложения перед Картой.
- **Жирный текст** — термины при первом упоминании, главная мысль абзаца.
  Максимум 1–2 выделения на абзац.
- **Спецблоки:** `> **Лайфхак:**`, `> **Важно:**`, `> **Демонстрация:**`.
  Только если материал явно есть в транскрипте.
- **Карта → Текст:** каждый буллет Карты содержательно раскрыт в Тексте —
  требование на входе субагенту, не только контроль на выходе.

**Почему не постфактум-форматирование бригадиром:**
Дополнительная нагрузка на бригадира. Правила на входе субагентам —
они применяют при написании, дешевле.

### Реализовано

widget_generator.py (build_reconstruction_html), layer2_widget.md,
tests/test_widget_generator.py, SKILL.md (ШАГ 2).
subagent_phase2.md (ключевая мысль, форматирование, блоки).
tov_spec.md (раздел «Форматирование»).
brigadir.md (шаблон + доп. проверка).
widget_skeleton.html, validate_widget.py, start_form.html, widget.md.

---

## 2026-05-14 — Единое форматирование прозы + вкладка «Траектория»

**Бэклог-айтемы:** Item 1 (форматирование прозы реконструкции) + Item 2 (траектория).

### Item 1: Единый подход к форматированию прозы

**Проблема:** проза `## Логическая реконструкция` в JSON-поле `prose` попадала в виджет
«сырым» текстом: бригадир не форматировал, layer2_widget.md говорил «без изменений»,
widget_generator.py оборачивал в голый `<p>`. Тогда как `body` и `right` проходили
полный цикл MD→HTML.

**Решение:** убрать исключение — применить к `prose` те же правила что к `body`.
Три файла:

1. **brigadir.md** — добавлены правила форматирования прозы (`**термин**`, абзацы,
   `> **Главная идея:**`)
2. **layer2_widget.md** — правило `prose`: «без изменений» → «конвертировать как body»
3. **widget_generator.py** — `parts = [f'<p>{prose}</p>']` → `parts = [prose]`

TDD: написан падающий тест `test_build_reconstruction_html_prose_passthrough`,
затем фикс.

### Item 2: Вкладка «Траектория»

**Идея:** бригадир пишет `## Траектория` в мастер-MD — по аналогии с `## Логическая
реконструкция`. Виджет читает этот раздел и рендерит как первый экран.

**Формат `## Траектория`:** проза (1–2 предложения о том, как спикер ведёт слушателя),
затем таблица (сегмент → тайм → ход → описание). Один сегмент — раздел пропускается.

**Таксономия ходов** (8 типов с возможностью свободной метки):
концепт, метафора, пример, демонстрация, практика (редко), маркетинг, вывод, вопрос.

**JSON поле `trajectory`** (новое): `{ prose, moves: [{segment, timing, move, description}] }`
или `null`.

**Виджет:** новая функция `build_timeline_html()` — пропорциональная горизонтальная
шкала, ширина блока = длительность / сумма × 100%, цвет по типу хода,
клик → `goTo(id)` переходит к сегменту.

**Цвета:** концепт `#2562B0`, метафора `#7B5EA7`, пример `#2E6E2E`,
демонстрация `#96580F`, практика `#B07020`, маркетинг `#C0442E`,
вывод `#5A7A5A`, вопрос `#6A8FAF`, прочее `#888888`.

### Итог изменений

| Было | Стало |
|---|---|
| Проза реконструкции — голый текст | Проза форматируется как body (MD→HTML) |
| Реконструкция — первый экран | Траектория — первый экран, реконструкция — второй |
| Нет `## Траектория` | Бригадир пишет раздел, виджет рендерит шкалу |

### Реализовано

brigadir.md (правила форматирования + раздел `## Траектория`),
layer2_widget.md (правило `prose` + поле `trajectory`),
widget_generator.py (убрана `<p>`-обёртка + `build_timeline_html()` + `goTo()` + CSS),
tests/test_widget_generator.py (8 тестов, все зелёные).

**6 коммитов:** `ee8722e`, `c44d247`, `0c3709e`, `0fd2d59`, `a2d5154`, `59ad76f`

---

## 2026-05-15 — Нарезка по времени + унификация пайплайна

### Что изменили

**Проблема 1 — Чанки слишком большие.**
`CHUNK_SIZE = 16_000` токенов = ~75–80 минут. Обработка одного чанка занимает
слишком долго; тайминг в транскрипте уже есть — логично резать по нему.

**Решение:** переключить `preprocessor.py` на время:
- `CHUNK_DURATION_SEC = 3600` (60 мин) — основной режим, если в транскрипте есть тайминг
- `CHUNK_SIZE_TOKENS = 12_000` (~60 мин в токенах) — фоллбэк, если тайминга нет
- `windows()` теперь выставляет точки реза через каждые 60 мин по таймштампам,
  в токен-режиме — по накоплению до 12K (старая логика)
- `info()` показывает длительность и выбирает порог в зависимости от режима
- Подпись режима в выводе: `[тайминг-режим]` / `[токен-режим]`

**Проблема 2 — Лёгкий режим занимает контекстное окно бригадира.**
При транскриптах ≤ 25K токенов Claude делал всё сам (сегментация + текст +
реконструкция), не оставляя места для качественной работы над логикой и траекторией.

**Решение:** убрать «лёгкий режим» как отдельный путь. Единый пайплайн:
- Сегментация всегда делегируется Фазе 1 (1 агент на один чанк или N параллельно)
- Бригадир всегда получает черновые карты Фазы 1 и занимается только
  высокоуровневой работой: глобальный обзор → чистовая карта → Фаза 2 → сборка

### Что изменилось

| Было | Стало |
|---|---|
| Чанки ~15–18K токенов (~75–80 мин) | Чанки ~60 мин (или ~12K токенов без тайминга) |
| Нарезка по токенам | Нарезка по таймингу, фоллбэк на токены |
| Лёгкий режим: Claude делает всё сам | Единый пайплайн: всегда Фаза 1 агент |
| Бригадир иногда сегментирует | Бригадир никогда не сегментирует |

### Реализовано

preprocessor.py (константы, `has_timestamps()`, `info()`, `windows()`),
SKILL.md (убран «Лёгкий режим», единый пайплайн, диаграмма, таблица),
brigadir.md (раздел «Роль» — убрана ссылка на лёгкий режим).

---

## 2026-05-15 (вечер) — Корневая правка постановки задачи Фазы 2

### Проблема

На реальном прогоне (9 сегментов, ~95 мин) 4 из 9 сегментов имели дефект:
субагент Фазы 2 писал кратким содержанием вместо раскрытого конспекта.
Текст выходил сопоставимым по объёму с Картой (~1× вместо целевых 2–3×).

Первая попытка лечения — добавить самопроверку «раскрытости буллетов Карты в Тексте».
Но это лечение симптома. Корень: задача субагенту была поставлена с ног на голову —
Карта подавалась как первичная, Текст как её развёртывание. При таком фрейме
субагент сжимал транскрипт в тезисы (Карта), а потом «разворачивал обратно» —
транскрипт фактически выпадал из процесса.

### Решение: перевернуть фрейм

Источник истины — транскрипт, а не Карта.

- **Текст** = очищенный транскрипт, сохраняющий голос автора. 70–90% объёма.
- **Карта** = извлечение оглавления из готового Текста, не задание для него.
- **Ключевая мысль** = итог после Карты.

Порядок работы субагента: транскрипт → Текст → Карта → Ключевая мысль.

### Почему не добавить самопроверку поверх старого фрейма

Симптом «субагент пишет кратко» — следствие неправильной постановки.
Самопроверка на «раскрытость» не убирает причину. Перевёрнутый фрейм снимает
проблему на уровне задачи — самопроверка становится проще и честнее.

### Что изменилось

**subagent_phase2.md** — переписан целиком:
- Новый раздел «Принцип постановки задачи»: транскрипт = единственный источник
- Порядок процесса и выводных блоков переставлен (Текст → Карта → Ключевая мысль)
- Таблица «убираем шум / сохраняем стилистику», принцип сомнения
- Обязательный блок `### Самопроверка` (содержание / стилистика / объём)
- Запрет «не пиши Карту первой» в разделе «Чего не делать»

**brigadir.md** — верификация переориентирована:
- Источник истины при верификации — транскрипт, не чистовая карта
- Три шага: чтение блока Самопроверки → выборочная сверка с транскриптом → запрос переделки
- Очистка: блоки `### Самопроверка` субагентов вырезаются из финального мастер-MD

### Реализовано

subagent_phase2.md (полная перезапись), brigadir.md (раздел «Верификация текста»,
раздел «Очистка перед сохранением»). Коммит `47b3684`.

---

## 2026-05-15 (ночь) — Самопроверки бригадира #1, #2, #4

### Задача

Добавить видимые самопроверки бригадира на трёх ключевых этапах пайплайна.
Главное ограничение: исключить саботаж — LLM не должна штамповать `✓` без реальной работы.

### Решение: анти-саботажный формат

Единый шаблон блока: `Что проверял / Находки / Действие / Итог`.
Поле «Что проверял» обязано содержать **конкретные единицы** (пары сегментов, номера,
цитаты), а не «всю карту». «Без находок» допустимо только при конкретно перечисленных
проверенных единицах.

### Три самопроверки

**Самопроверка №1** (после Фазы 1, перед чистовой картой) — взгляд читателя:
1. Тема сегмента называется одним словом?
2. Два смысловых блока в одном сегменте?
3. Соседние пары об одном? → слияние
4. Цепочка 3+ коротких сегментов = искусственное дробление?

При конфликте с инженерными критериями (единство роли, сигналы смены режима,
связность) — инженерные побеждают. №1 — финальный фильтр поверх них.

**Самопроверка №2** (после чистовой карты, перед Фазой 2) — карта целиком:
1. Заголовки подряд = логичное оглавление?
2. Неконтактные повторы (сег. 1 и 7 об одном)?
3. Темы из черновых карт Фазы 1 не выброшены при гармонизации?
4. Крайний дисбаланс объёмов (порог 5×)?

**Самопроверка №4** (после сборки мастер-MD, перед сохранением) — финальный гейт:
1. Цельная история (нет нестыковок между сегментами)?
2. Нет явных повторов в соседних сегментах?
3. Нет провалов (заявлено в реконструкции, но не раскрыто)?
4. Переходы соответствуют содержанию следующего сегмента?
5. Траектория и Реконструкция опираются на реальное содержание?

### Почему убрана самопроверка #5 (Логическая реконструкция)

Самопроверка-гейт — симптоматическое лечение. Вместо неё — усиленная инструкция
«Принцип абзаца»: «этот абзац — про риторику всего выступления одной мыслью,
не саммари таблицы». Атакует корень при написании, а не проверяет постфактум.
Токены не тратятся на output блока проверки.

### Стоимость #4

Мастер-MD уже в контексте бригадира после сборки → input бесплатен.
Платится только output блока (~1–3K токенов). На 5-часовом выступлении — <$0.05.

### Реализовано

brigadir.md: шесть вставок (новый раздел `## Формат самопроверок`, шаги №1 и №2
в `## Глобальный обзор`, «Принцип абзаца» в `## Логическая реконструкция`,
новый раздел `## Финальная самопроверка мастер-MD`, обновление «Очистки»).
Коммит `71a0d9c`.

---

## 2026-05-16 — Оптимизация Фазы 2: отрезок сегмента вместо целого чанка

### Задача

Сократить дублирование контекста в Фазе 2. Каждый writer получал полный
`[CHUNK]` (~12K токенов, ~60 мин транскрипта), хотя писал один сегмент
(~12 мин). На лекции 2 ч с 10 сегментами это ~120K токенов суммарно только
на `[CHUNK]`, бóльшая часть — данные за пределами сегмента.

### Что рассматривали

**Вариант A — резать `[CHUNK]` до границ сегмента, M агентов как сейчас.**
Per agent: `[CHUNK_сегмента]` ~2400 ток. вместо ~12K.

**Вариант B — 1 агент = чанк, пишет все свои сегменты.**
N=2 агента вместо M=10. Сильнее экономит на накладных (инструкции / NAV / TOV
расшариваются между сегментами внутри одного агента).

**Вариант C — уменьшить размер чанков с 60 до 40 мин.**

### Сравнение токенов (база: 2-часовая лекция, M=10 сегментов)

| Схема | Input Фазы 2 | Экономия | Wall-clock | Риски качества |
|---|---:|---:|---|---|
| Текущая | 164 000 | — | 1× | — |
| A (отрезок сегмента) | 68 000 | ~58% | 1× | — |
| B (1 агент = чанк) | 33 600 | ~80% | 4–5× медленнее | Деградация в длинном output, слабее самопроверка, retry в 5–6× дороже |

Уменьшение чанков (Вариант C) для A даёт 0 экономии (размер `[CHUNK_сегмента]`
определяется сегментом, а не чанком) и добавляет накладные на нарезку
и работу бригадира на стыках. Отвергнут.

### Решение: Вариант A

Сэкономили основное на `[CHUNK]` без потери скорости и качества. Остаток
накладных (инструкции `subagent_phase2.md`, NAV MAP, TOV SPEC) дублируется
M раз — но единственный путь к нему — Вариант B с его минусами. Не стоит
жертвовать качеством ради дополнительных ~22 п.п.

### Реализация

- `preprocessor.py`: новая команда `slice <чанк> --from HH:MM:SS --to HH:MM:SS` —
  печатает строки чанка в диапазоне таймштампов сегмента.
- `SKILL.md`: раздел «Фаза 2» — перед запуском writer'ов прогнать `slice`
  для каждого сегмента из чистовой карты, подавать `[CHUNK_сегмента]`
  вместо `[CHUNK]`.
- `subagent_phase2.md`: переименован вход `[CHUNK]` → `[CHUNK_сегмента]`,
  отмечено что это уже вырезанный отрезок.

Фаза 1 без изменений — там агент по-прежнему получает чанк целиком
(он ищет границы сегментов).

---

## 2026-05-17 — Радикальная оптимизация по токенам (10 пунктов, 57–64% экономии)

### Задача

Прогон ч2 курса Велса съел ~650–700K токенов на 1:33 контента — целый 5-часовой
rate-limit Claude Code. Аудит зафиксировал слабые звенья: дублирующее чтение спек
6 субагентами Ф2 (~54K), output Ф2 (~297K, две самопроверки занимают треть и
дают «менять ничего не нужно» в 6/6 случаев), чтение чанка целиком ради ToV (~18K
при потребности ~3K), Opus на rewrite-задачах субагентов.

Параллельно — методичка сегментации не справилась с ч2: 21 сегмент на той же
длине, где в ч1 было 8. Пользователь укрупнял вручную.

### Брейншторм → радикальная переработка

Первая редакция аудита предлагала точечные оптимизации на 15–20%. В брейншторме
пользователь сам инициировал переход на Sonnet («не имеет смысла гонять Opus на
rewrite-задачах») — это превратило точечную оптимизацию в радикальную (57–64%).

**10 пунктов спеки** (`docs/superpowers/specs/2026-05-17-konspekt-token-optimization.md`):

1. Читательская самопроверка Ф2 → одна на бригадире.
2. Спеки Ф2 встроены в промт (нет Read внутри субагента); `subagent_phase2.md`
   и `tov_spec.md` сокращены ~×2.
3. **Sonnet для Ф1 и Ф2** через явный `model: "sonnet"` (главный рычаг).
4. Гибридный ToV: бригадир проверяет `transcripts/tov_<серия>.md`, если нет —
   запускает `preprocessor.py head` и сохраняет файл для следующих частей серии.
5. Дисциплина бригадира: запрет TodoWrite, перечитывания чанков после Ф1,
   лишних deferred tools.
6. **Заморозка раздела «Траектория»** — HTML-комментарий `<!-- FROZEN: ... -->`
   вокруг живого раздела, удаление из пайплайна и шаблона.
7. Методичка сегментации: «уровень главы курса» вместо «параграфа», анти-маркеры
   (смена формата ≠ граница), тест на укрупнение, серийный контекст как эталон.
8. Подзаголовки `####` внутри Текста — детализация навигации переезжает на
   уровень подразделов. Карта = тезисы, `####` = тематические разделы.
9. Новые специальные блоки `> Ключевая идея`, `> Принцип` — стратегический
   уровень подсветки.
10. Уровни **(И)/(М)/(Д)** на каждом буллете Карты + блок `> Методология` —
    иерархия strategy/tactics/operations.

### Реализация: subagent-driven, Sonnet на исполнителях

Координатор Opus (главный агент), все имплементеры и ревьюеры — Sonnet через
`subagent-driven-development`. Полный двухстадийный цикл на каждую задачу:
spec-compliance review → code-quality review. Экономия даже на самой реализации.

12 коммитов в трёх волнах:

- **Task 1** (preprocessor.py `head`) + fixup off-by-one для длинной первой строки.
- **Task 2** (subagent_phase1.md — методичка сегментации, анти-маркеры).
- **Task 3** (subagent_phase2.md — полная переработка) + fixup «пять блоков»
  (опечатка планировщика «четыре»).
- **Task 4** (tov_spec.md — сокращение 138→85 строк).
- **Task 5** (brigadir.md — 15 шагов в одном файле: Дисциплина, Запуск Ф1,
  Подготовка к Ф2, FROZEN Траектория, методичка, шаблон с (И)/(М)/(Д)),
  плюс fixup синхрона с заморозкой (упоминания Траектории в живых разделах
  закомментированы; `[TOV_BASE]` → `[TOV SPEC]`; subagent_phase1.md больше не
  читает audience_spec.md сам — встраивается бригадиром как `[AUDIENCE]`).
- **Task 6** (SKILL.md — Sonnet для Ф1/Ф2, гибридный ToV, дисциплина).
- **Task 7** — финальная сверка + handoff. Финальный full-review всей
  реализации поймал ещё один минор (`[AUDIENCE]` не назван в списке переменных
  Ф1) — закрыт коммитом `5e50978`.

### Уроки

**Брейншторм перед реализацией важнее, чем кажется.** Первая редакция аудита
была «оптимизация в лоб» на 15–20%. Один комментарий пользователя про Sonnet
утроил эффект — реализация плана сама стала экономичной, а итоговая экономия
вышла за 50%. Цена брейншторма — 1 сессия, отдача — отсутствие переделок.

**План задаёт точный код, но не отменяет review.** В Task 1 reviewer нашёл
off-by-one в функции, скопированной побайтово из плана. В Task 3 нашёл опечатку
«четыре блока» вместо «пять». В Task 5 finальный full-review поймал упущенные
упоминания Траектории в живых разделах и неконсистентность `[TOV_BASE]` vs
`[TOV SPEC]`. Subagent-driven с двумя ревью на каждую задачу окупается даже
при формально «механической» имплементации.

**Subagent-driven для Markdown-правок тоже окупается.** Опасение «зачем
двухстадийный цикл на правки промтов» оказалось напрасным: ревью каждой задачи
ловило конкретные дефекты, которые имплементер не замечал. Без них на тестовый
прогон ушли бы рассинхроны между файлами.

### Файлы

- Спека: `docs/superpowers/specs/2026-05-17-konspekt-token-optimization.md`.
- План: `docs/superpowers/plans/2026-05-17-konspekt-token-optimization.md`.
- Handoff на тестовый прогон: `docs/konspekt-optimization-test-handoff.md`.

### Известные риски

- `preprocessor.py head` не парсит чисто SRT-формат (рассчитан на `[HH:MM:SS]`,
  а `Урок 2.srt` — `00:00:00,000 --> 00:00:30,000`). Команда отрабатывает,
  но в выводе остаются остатки таймштампов. Не блокирует прогон, но ToV-выборка
  будет чуть зашумлена.
- `layer2_widget.md` ждёт раздел `## Траектория` мастер-MD как источник
  `trajectory`. После заморозки виджет получит `trajectory: null`. Решение —
  следующая итерация (либо разморозка, либо обновление виджет-генератора).

---

## 2026-05-19 — Парсер мастер-MD: убрали ручную сборку JSON для виджета

### Задача

Слой 2 (виджет) собирался в два шага: автор вручную переносит содержание
мастер-MD в JSON (~15 минут на виджет, с риском пропустить жирное или
перепутать метку блока), потом `widget_generator.py JSON → HTML`. Промежуточный
JSON — лишний «переводчик»: любая правка мастер-MD молча устаревает виджет,
ошибки переноса не ловятся.

Цель: убрать ручной шаг. `python widget_generator.py master.md` → HTML
напрямую. JSON-режим оставлен резервным (тонкие правки виджета без правки MD).

### Реализация: subagent-driven, Codex 5.5 на исполнителе

Координатор Opus (главный агент) написал план в 13 задач — каждая = чистая
функция парсера + sanity-check + commit. План передан Codex 5.5 на исполнение
через `superpowers:executing-plans` для экономии токенов. Перед передачей —
повторный проход по плану свежим глазом, нашли и поправили дыру: пробел
между `</strong>` и `<ol>/<ul>` в blockquote-блоках с нумерованным списком
терялся, эталон требовал пробел.

Параллельно — наведение порядка в git (7 коммитов перед стартом агента):
зафиксировали накопленные правки `widget_generator.py` (вертикальный
таймлайн), `layer2_widget.md` (таблица цветов меток, блок промпта),
`SKILL.md` (правило «жирные якоря»), backlog, history, settings.

13 коммитов парсера (`0921536…1e8f13f`):

- **Tasks 1–11** — функции парсера в `md_parser.py`:
  `_split_sections`, `_parse_meta`, `_apply_inline`, `_label_color`,
  `_render_blockquote`, `_render_prompt`, `_parse_text`, `_parse_map`,
  `_parse_segment`, `_parse_reconstruction`.
- **Task 12** — диспетчер по расширению в `widget_generator.py`:
  `.md` → парсер, `.json` → как раньше. Старый путь полностью сохранён.
- **Task 13** — `layer2_widget.md` обновлён: основной путь через мастер-MD,
  JSON помечен как резервный.

### Контроль результата

Эталонный мастер-MD прогнан через парсер, dict побайтово сравнён с эталонным
JSON: meta совпадает, prose реконструкции `1318`/`1318` символов, table 3/3,
3/3 сегмента (id/type/title/timing), body сегментов 1 и 3 байт-в-байт,
right всех трёх сегментов байт-в-байт, 6 промптов с id `cpbp1`–`cpbp6`
сквозными. В body сегмента 2 разница ровно 2 байта — пропущенный союз
«И в этом» в эталоне (ошибка ручной сборки JSON, парсер взял правильный
текст из MD).

Визуальная проверка в браузере — виджеты практически идентичны.

### Уроки

**Брейншторм + ревью плана окупились в 2 байта.** Codex 5.5 на исполнении
плана — отличный фит для механической имплементации по подробной спеке.
Все 13 задач прошли без переделок, потому что план был детальный (полный код
в каждом шаге) и потому что перед передачей был ещё один проход по плану
свежим глазом, поймавший дыру с пробелом перед списком. Цена ревью плана —
15 минут моего времени, отдача — 0 итераций фикса у Codex.

**Имя выходного файла длиннее — это норма.** Парсер строит `out` как
`Виджет — <stem без _мастер>.html`. Эталонный JSON ручной сборки давал
короткое имя `Урок 2.html`, парсер даёт полное `...Урок 2. Пошаговый
алгоритм + личный проект.html`. Однозначно выводится из имени файла,
эвристик не требует — оставили.

**Мастер-MD стал единственным источником правды.** Любая правка MD
теперь автоматически отражается в виджете одной командой. Расхождение
в «И» — это и есть ценность рефакторинга: ручная сборка давала случайные
пропуски, парсер их не делает.

### Файлы

- Handoff: `docs/2026-05-19-konspekt-md-parser-handoff.md`.
- План: `docs/superpowers/plans/2026-05-19-konspekt-md-parser.md`.
- Парсер: `.claude/skills/konspekt/md_parser.py`.

### Известные риски

- Метка blockquote с двумя `:` внутри (`**Пример: вот так:**`) сломает
  регулярку — упадёт с `MasterMDParseError`. В текущих мастер-MD таких нет.
- HTML-escape прозы не делается (внутри fenced — делается). Если в будущем
  в прозе появятся литеральные `<`/`>`/`&` — попадут в HTML как есть.
- Парсинг badge зависит от структуры названия `Курс. Модуль N. Урок K: тема`.
  Названия с > 3 частями до `:` дадут только первые две части в badge.

---

## 2026-05-20 — Главная мысль ДО сегментации

Прогон одноагентного скилла на коротком уроке (Ледовских, GitHub-блок Урока 3,
12 минут): мастер-MD + виджет за один проход. По итогам — методологическая
правка скилла.

### Что хотели исправить

Сегментация строилась «снизу» — от тематических границ транскрипта.
Главная мысль всего видео появлялась только в блоке `> **Главная идея:**`
Логической реконструкции, которая пишется в самом конце. К этому моменту
сегменты уже нарезаны и могут не работать на единую риторическую идею —
получаются «сегменты ради сегментов».

Пользователь сформулировал: главная мысль должна определяться **до**
сегментации, и от неё надо плясать.

### Что сделали

`.claude/skills/konspekt/SKILL.md` — 4 точечные правки:

1. **ШАГ 1 разбит на 1.1 / 1.2 / 1.3.** 1.1 — чтение. 1.2 — новый этап
   «Главная мысль и заголовок»: формулировка риторической позиции автора
   (не темы) в 1–2 предложения + 2–3 варианта заголовка с разными
   акцентами, пользователь выбирает. 1.3 — сегментация с правилом
   «каждый сегмент раскрывает грань главной мысли; иначе — служебка
   или мысль сформулирована узко».
2. **Шапка файла:** добавлена строка `**Главная мысль:**` между
   `**Профиль:**` и `**Сегментов:**`.
3. **ШАГ 2:** новый пункт «Если по ходу главная мысль уточнилась».
   Стилистическое уточнение — обновить молча. Смысловое (другая
   риторическая рамка) — остановиться, сообщить пользователю,
   переспросить заголовок.
4. **ШАГ 3 «Самопроверка»:** новый ориентир — «Сегменты раскрывают
   главную мысль из шапки или есть лишние, которые на неё не работают?».

### Демо-прогон

Первый материал по новому процессу — урок про GitHub:

- Главная мысль: «Как не потерять наработки — GitHub превращает любую
  ошибку Claude или сбой ноутбука в обратимое событие через две речевые
  команды и откат к рабочей версии».
- Из 3 предложенных заголовков выбран «GitHub: чтобы всегда можно было
  вернуться к рабочей версии» (акцент на пользу для слушателя).
- 3 сегмента (зачем GitHub / работа через Claude / итог серии и ДЗ)
  явно раскрывают грани главной мысли.

Файлы: `transcripts/archive/Вайбкодинг. М1-У3-4. GitHub_мастер.md`,
`transcripts/archive/Виджет — Вайбкодинг. М1-У3-4. GitHub.html`.

### Ключевой урок

Главная мысль — не итог анализа, а **постановка задачи** для сегментации.
Если её не зафиксировать до того, как нарезаешь сегменты — потом она
получится «общими словами поверх таблицы», как и предупреждал «Принцип
абзаца реконструкции» в текущем скилле. Этот «Принцип абзаца» лечил
симптом — теперь его корень закрыт сверху по пайплайну, в ШАГ 1.2.

---

## 2026-05-21: кастомизация экрана реконструкции виджета (прототип Слоя 3)

Прогон скилла на М2-У7 (7 сегментов, 22 мин). По итогам пользователь
указал на системную проблему первого экрана виджета («Логическая
реконструкция»): текст одним кирпичом, сложным языком, без визуальных
пауз; правая колонка пустая (`<div class="insights"></div>` из
`widget_generator.py:446`). Блок не работает на свою задачу — не даёт
читателю мгновенную картину «о чём это видео».

### Что прототипировали

Два одноразовых patch-скрипта в корне проекта, по одному на лекцию:

- `patch_recon_m2u7.py` — М2-У7. SVG: горизонтальная цепочка
  `research.md → brief.md → index.html → index.html+` со стрелками
  «промпт 1/2/3+4» над ними и оранжевыми кружками ✎ «правка» между
  шагами (обязательная человеческая правка между промптами).
- `patch_recon_m2u6.py` — М2-У6. SVG: 5 агентов-разведчиков параллельно
  сходятся в `research.md`, оттуда же 3 эксперта-критика расходятся,
  пунктирной стрелкой обратной связи дописывают раздел «Экспертная
  оценка» в тот же файл.

Парсер `md_parser.py` не трогали. Кастомизация — post-processing патчем
поверх готового виджета: адресная замена внутри `var BODY = {...}`
и `var RIGHT = {...}` по ключу `"00"`.

### Принципы кастомного экрана реконструкции

- **Левая колонка** — короткие абзацы прозой (1 абзац = 1 мысль),
  опциональная **SVG-схема** в центре (если в лекции есть
  операциональная схема — цепочка артефактов, параллельные процессы,
  цикл, иерархия; иначе не делать), блок «Главная идея» в конце.
  Таблица «#/роль/ход» убрана — дублировала прозу.
- **Правая колонка** — «артефакты урока»: секции с цветными карточками
  (Файлы / Промпты / Лайфхаки / Ссылки / Домашка). Состав секций
  под содержание лекции, не догма.
- **Раскрывающиеся `<details>` для промптов** — заголовок + краткое
  описание видны всегда, по клику разворачивается дословный текст
  промпта с кнопкой «копировать». Реализовано нативно, без JS;
  стрелка `▶ → ▼` через scoped CSS на `[open]`.
- **Кликабельные `↗`** — переходят к нужному сегменту через готовую
  функцию `goTo()` движка виджета (`widget_generator.py:302–304`).
  Правило: если все элементы секции про один сегмент — стрелка на
  заголовке секции; если разные — стрелки у каждого элемента отдельно.

### Эволюция решения по ходу сессии

Несколько итераций, каждая корректировала направление:

1. **ASCII-схема в `<pre>`** — кириллица + псевдографика в Manrope
   рассыпались в узкой колонке, выглядели дёшево. Перешли на SVG.
2. **Карточки сегментов справа** («1 — ТЗ как чертёж», «2 — Прочитайте
   и поправьте» и т.д.) — пользователь забраковал: «фразы выглядят
   оторванными, выдернутыми, неясен контекст». Жанр «карточка = бирка
   к бирке». Поняли: правая колонка не должна дублировать левую,
   она должна быть другим срезом материала.
3. **Артефакты урока** вместо карточек сегментов — операциональный
   список (что физически появится в проекте после просмотра): файлы,
   промпты, лайфхаки, ссылки, домашка. Заработало.
4. **SVG вместо ASCII** — пользователь оценил как «намного интереснее».
5. **Раскрывающиеся промпты** — попросил, чтобы тексты были прямо в
   правой колонке. Реализовано через `<details>/<summary>`.
6. **Кликабельные `↗`** — пришли итеративно. Сначала сделал групповые
   ссылки на заголовках секций; пользователь поправил, что в М2-У7
   «Файлы/Промпты/Лайфхаки» содержат привязки к разным сегментам.
   Перевёл на индивидуальные стрелки.

### Ключевые уроки

- **Жанр карточек справа важнее их содержания.** «Карточка по сегменту»
  даёт ощущение бирки. «Карточка по артефакту» — ощущение инвентаря.
  Тот же визуальный формат, разный жанр восприятия.
- **SVG обязательно вместо ASCII в этом дизайне.** Кириллица и
  моноширинная псевдографика несовместимы с минималистичным виджетом
  в Manrope.
- **`<details>` достаточно для раскрытий.** JS не нужен. Поворот стрелки
  решается одной строкой scoped CSS через `[open]`.
- **`event.stopPropagation()` критично для вложенных кликов.** Стрелка
  `↗` внутри `<details>` обязана останавливать всплытие, иначе клик
  на ней разворачивает блок, а не переходит в сегмент.
- **Самопроверка результата перед отдачей.** В первой версии
  `patch_recon_m2u7.py` regex нашла первое вхождение `"00":` в файле,
  но это оказался `RIGHT`, не `BODY`. Замена ушла не туда, проза и
  карточки поменялись местами. Не проверил визуально, отдал
  пользователю — он увидел и поправил. Исправил адресной заменой
  внутри явно найденного блока (`var BODY {...}` vs `var RIGHT {...}`).

### Что НЕ автоматизируется

- **Конкретный текст прозы** — это смысл, не форма.
- **Конкретный SVG** — творческое решение под лекцию. Лекции бывают
  разной риторической формы (цепочка / параллели / цикл / иерархия /
  тезис без схемы). Можно дать **палитру типовых форм** как ориентир,
  но генератор «автосхемы из мастер-MD» — путь к плохим схемам.
- **Конкретные привязки `goto_id`** — знание о структуре урока.

### Handoff: вшить в скилл как Слой 3

После того как два виджета признаны хорошими, пользователь попросил
вынести формат в скилл — как глобальную фишку, не одноразовый патч.
Решение: новая сессия (контекст текущей перегружен; плюс задача
другого типа — не «продукт лекции», а «инструмент скилла»).

Handoff в `docs/konspekt-layer3-handoff.md`. План:
- `.claude/skills/konspekt/layer3_recon.md` — инструкция для агента
  (по образу `layer2_widget.md`).
- `.claude/skills/konspekt/recon_patch_template.py` — шаблон для
  копирования (`patch_block`, `DETAILS_CSS`, хелперы
  `section/file_card/prompt_card/link_card` — всё, что одинаково
  в обоих наших патчах).
- `SKILL.md` упомянет Слой 3.
- Парсер не трогать. Кастомизация остаётся post-processing патчем.

---

## 2026-05-21 — Вшивание Слоя 3 в скилл: layer3_recon.md + recon_patch_template.py

### Задача

Реализовать handoff из `docs/konspekt-layer3-handoff.md`: выделить из
одноразовых patch-скриптов М2-У6/У7 переиспользуемый компонент скилла.

### Что создано

**`.claude/skills/konspekt/layer3_recon.md`** (по образу `layer2_widget.md`) —
инструкция для агента в чистой сессии:

- Назначение Слоя 3; когда применять.
- Принципы левой/правой колонки: проза короткими абзацами + опц. SVG +
  «Главная идея» слева; секции с карточками (Файлы/Промпты/Лайфхаки/
  Ресурсы/Домашка) под содержание лекции справа — не догма.
- Правило кликабельности `↗`: если все элементы секции → один сегмент —
  стрелка на заголовке секции; если разные сегменты — стрелки у каждого
  элемента отдельно.
- SVG: когда нужен/не нужен, пять типовых форм с референсами.
- Шесть операциональных шагов от чтения мастер-MD до запуска патча.
- Что уже есть в движке виджета (`goTo()`, `cp()`) — не изобретать заново.

**`.claude/skills/konspekt/recon_patch_template.py`** — шаблон для копирования:

- Переиспользуемые куски: `patch_block`, `DETAILS_CSS`, хелперы
  `section`, `file_card` (М2-У7-версия, с `goto_id`), `prompt_card`
  (с `goto_id` и `event.stopPropagation()`), `link_card`.
- TODO-заготовки для лекция-специфичных частей.
- **Охранный запуск:** шаблон считает количество `# TODO` в самом себе;
  если > 0 — `sys.exit()` с сообщением «найдено N незаполненных TODO-секций».
  Проверено: 9 незаполненных TODO → выход с сообщением, файл не изменён.

**`SKILL.md`** — добавлен раздел «Слой 3: кастомизация экрана реконструкции».

### Ключевые решения

**Шаблон для копирования, не библиотечный модуль.** Patch-скрипты эфемерны
(один на лекцию, написал → запустил → забыл). Каждый patch самодостаточен;
импорт лишняя сложность для одноразового файла.

**Охранный запуск через подсчёт `# TODO` в исходнике.** Шаблон синтаксически
корректен, но упадёт с понятным сообщением до любых изменений файлов.
Проще `raise NotImplementedError` в каждой заглушке — один централизованный
гейт вместо разрозненных.

**Хелпер `file_card` унифицирован по М2-У7** (с `goto_id`). М2-У6 использовал
упрощённую версию без стрелки. Шаблон берёт более полный вариант.

**SVG не автоматизируется.** Содержание схемы — творческое решение под лекцию.
Типовые формы в `layer3_recon.md` — ориентир, не генератор.

---

## Сессия 2026-05-21 (вечер) — применение Слоя 3 к М1-У5-2 (Документация как память проекта)

Третий виджет с кастомным экраном реконструкции — урок Ледовских про три файла-памяти (PROJECT, CLAUDE.md, PLAN) из архивной папки `transcripts/archive/`. К этой сессии Слой 3 уже был вшит в скилл (`.claude/skills/konspekt/layer3_recon.md` + `recon_patch_template.py` существуют, untracked); сегодня — первое полноценное применение «как пользователь» по инструкции скилла.

### Что сделано

Создан `patch_recon_m1u5-2.py` по шаблону `recon_patch_template.py`, применён к виджету в `transcripts/archive/Виджет — Вайбкодинг. М1-У5-2. Документация как память проекта.html`. In-place замена `var BODY["00"]` и `var RIGHT["00"]` через `patch_block()`.

**Левая колонка** — 5 коротких абзацев + SVG-схема + блок «Главная идея». SVG показывает механику работы памяти: «новый чат без контекста» → три файла (CLAUDE.md/PROJECT/PLAN) → «Claude сразу в контексте проекта», плюс нижний ярус «как файлы появляются» (вы → промпт → Claude → .md) с возвратной петлёй «раскритикуй и улучши».

**Правая колонка** — 4 секции с индивидуальными `↗`-привязками к сегментам:
- Три файла памяти (PROJECT/CLAUDE.md/PLAN → сегменты 03/04/05)
- Промпты (5) с раскрывающимся текстом → 03/04/05/05/06
- Лайфхаки (4) → 03/04/05/06
- Что сделать после урока — 6-шаговая ДЗ → 06

### Подтверждения по итогам

- **Шаблон работает как задумывалось.** Один файл копируется в `patch_recon_<урок>.py`, заполняется по разделам, запускается. Никакого «дрейфа» от референсов М2-У6/У7 — структура шаблона и трёх живых патчей идентична.
- **SVG-форма «механизм памяти» — новая.** В референсах были «параллельные ветви» (М2-У6) и «горизонтальная цепочка» (М2-У7); сегодня добавилась форма «проблема → артефакты → результат + цикл правок». В таблицу типовых форм в `layer3_recon.md` пока не вписана — добавится после ещё 1–2 повторений.
- **Виджеты из `transcripts/archive/` обрабатываются той же командой.** Патч ходит по абсолютному пути и не зависит от того, где лежит HTML. Архивированные уроки можно дозревать Слоем 3 без переноса в основную папку.

### Файлы сессии

- `patch_recon_m1u5-2.py` в корне.
- Обновлённый виджет: `transcripts/archive/Виджет — Вайбкодинг. М1-У5-2. Документация как память проекта.html`.

---

## Сессия 2026-05-23 — правило жирного, технический минимум реконструкции, виджет М2-У10-1 со Слоем 3

Большая сессия по [docs/konspekt-handoff-2026-05-23-fix-bold-and-layer3.md](konspekt-handoff-2026-05-23-fix-bold-and-layer3.md). Три направления: переписать правило жирного, зафиксировать правило Слоя 3 чтобы не дать ему деградировать, сделать виджет М2-У10-1 со Слоем 3.

### Правило жирного: с количественного на качественное

Старое правило «1–2 коротких якоря по 1–3 слова на абзац» оказалось самопротиворечивым: «якорь — смысловая опора абзаца» и «1–3 слова» — две разные модели. Пара слов передаёт тему («о чём абзац»), но не смысл («что в абзаце сказано»). На мастер-MD М2-У10-1 получилось пёстро: выделялись слова-метки («23 раза», «билборд», «Telegram»), которые не складывались в канву смыслов.

**Новое правило (закреплено в `SKILL.md`, раздел «Навигация в Тексте»):** жирное = **сжатая формулировка ключевой мысли абзаца/значения/буллета**. Длина определяется полнотой смысла, не лимитом (часто 4–8 слов). Главный тест: прочитать только жирное во всём сегменте сверху вниз — складывается ли канва смыслов? Если разрозненные слова — выделение «по форме правильное», но не работает.

**Структурные пометки (`**Ключевая мысль:**`, `**Тип:**`, `**Кейс 1:**`, ярлыки в `> ...`-блоках) остаются жирными для фокусировки**, в счёт лимита абзаца не идут. **В списках «заголовок — раскрытие»** заголовок до тире/двоеточия жирный (это уже структура буллета).

Memory `feedback_konspekt_bold_navigation.md` синхронизировано. В `Why` добавлен третий обучающий эпизод 2026-05-23 — «попытка ввести количественное правило провалилась как самопротиворечивая».

Мастер-MD М2-У10-1 перегенерирован с новым правилом. Парсер: EXIT 0, 6 сегментов, 0 меток (И)/(М)/(Д).

### Технический минимум реконструкции и метки (И)/(М)/(Д)

В этой же сессии закреплено решение из предыдущей: раздел `## Логическая реконструкция` в мастер-MD = **только** цитата `> **Главная идея:**` (дословная копия из шапки) + таблица сегментов. Прозы нет — она ушла в работу агента как «мысленная сборка сквозной цепочки И→М→Д».

Метки `(И)/(М)/(Д)` в Карте — **внутренний инструмент агента**, в финальный файл не пишутся.

### Слой 3: правило «дуга → реконструкция», не пересказ

После предыдущей сессии возник риск: раньше Слой 3 (`layer3_recon.md`) работал в логике «взять прозу реконструкции из мастер-MD и сжать до 4–6 абзацев». Прозы больше нет — без явного правила агент скатится в плоский пересказ «абзац за сегмент».

**В `layer3_recon.md` (раздел «Левая колонка») добавлено:**
- Иерархия источников для левой колонки (Главная мысль шапки → Ключевые мысли → Карта → Текст → Таблица).
- Принцип: **левая колонка формулирует риторическую дугу всего выступления, опираясь на Главную мысль шапки как фундамент. Это смысловое разворачивание Главной мысли через материал сегментов — не пересказ сегментов абзац-за-абзацем.**
- Антипаттерны (пересказ сегментов по порядку, общая формула без конкретики, потеря Главной мысли как ведущей нити).

### Слой 3 для М2-У10-1 и фидбек про язык

Применил Слой 3 к М2-У10-1 (`patch_recon_m2u10-1.py`). SVG не делал — лекция-разбор без операциональной схемы. Правая колонка: «Когда Mini App, когда ссылка» (С5), «Где работают Mini App» (С6), «Что дальше» (С5).

**Левая колонка переписывалась трижды.** Первая версия — пересказ сегментов 1→2→3→4→5→6 по порядку (тот самый антипаттерн). Вторая — литературоведческий разбор с «риторической дугой», «фундаментом», «глубинным зачем». Третья — простым языком, как объясняешь другу: о чём урок → что такое Mini App → зачем → главное предупреждение → где работает.

**После третьей версии — два правила в `layer3_recon.md`:**
- Цель левой колонки: **взгляд на урок сверху для читателя**. Не литературоведческий разбор «как устроено выступление», а карта урока — что хочет донести автор.
- **Простой разговорный язык** без оборотов «риторическая дуга», «фундамент держит», «глубинное зачем». Тест: прочитать абзац вслух — звучит как человек объясняет, или как преподаватель риторики? Если второе — переписать.
- В антипаттернах: «пересказ сегментов по порядку» (с признаком «можно подписать номер сегмента и совпадёт»), «потеря поворота» (если в Главной мысли есть «но» — должен быть отдельный абзац разворота), «литературоведческий разбор» (с конкретными примерами оборотов).

Новое memory: `feedback_plain_language.md` — общее правило простого языка для любых пользовательских текстов (не только Слой 3).

### Оптимизация скилла — план для отдельной сессии

Ретроспективно: перегенерация мастер-MD заняла много времени из-за качественного правила (мысленная работа на каждом абзаце) и большого Write (~9 KB). Что можно зафиксировать **системно** для всех будущих конспектов — собрано в [docs/konspekt-optimization-2026-05-23.md](konspekt-optimization-2026-05-23.md):

- **C.** Серийный контекст читать выборочно (Карты + Ключевые мысли, не Тексты) — экономия до 30K токенов на сессию для длинных серий.
- **A.** Стереотипные правила выделения в типовых блоках (`> Ключевая идея`, `> Принцип`, `#### Различие N`).
- **D.** Микро-шаг «карта выделений» перед записью сегмента.
- **B.** Сжать SKILL.md (~390 → ~280–300 строк) — отложить на 2–3 сессии применения нового правила жирного.

### Ключевые уроки

- **Качественное правило ≠ количественное.** Когда правило задаёт форму («1–2 якоря, 1–3 слова») — оно быстрое в применении, но ломается на смысле. Когда задаёт смысл («сжатая мысль абзаца») — оно работает, но требует мысленной работы на каждом абзаце. Это не баг, это плата за результат.
- **Структурные пометки и навигационные якоря — две функции, не одна.** Структурная пометка (`**Кейс 1:**`) помогает глазу прыгать по структуре файла. Навигационный якорь (`**Mini App = обычный сайт в новой обёртке**`) помогает понять смысл. Не нужно их складывать в один счётчик.
- **Реконструкция = взгляд сверху, не пересказ и не литературоведческий разбор.** Самый простой признак, что я пишу не для пользователя, а для себя: появились обороты «дуга разворачивается», «фундамент держит», «глубинное "зачем"». Это анализ для агента, а не текст для читателя.
- **Анализ устройства материала ≠ текст для пользователя.** Когда я разбираю «как устроен урок», в голове рождается язык анализа — и я по инерции выливаю его в финальный текст. Ловить разрыв осознанно.

### Файлы сессии

- Мастер-MD: [transcripts/Вайбкодинг. М2-У10-1. Зачем приложения в мессенджерах. Mini App vs ссылка на сайт_мастер.md](../transcripts/Вайбкодинг.%20М2-У10-1.%20Зачем%20приложения%20в%20мессенджерах.%20Mini%20App%20vs%20ссылка%20на%20сайт_мастер.md).
- Виджет: `transcripts/Виджет — Вайбкодинг. М2-У10-1. Зачем приложения в мессенджерах. Mini App vs ссылка на сайт.html` (со Слоем 3).
- Патч: [patch_recon_m2u10-1.py](../patch_recon_m2u10-1.py) в корне.
- План оптимизации: [docs/konspekt-optimization-2026-05-23.md](konspekt-optimization-2026-05-23.md).
- Memory: `feedback_konspekt_bold_navigation.md` (обновлено), `feedback_plain_language.md` (новое).

---

## Текущее состояние

**Файлы скилла:** SKILL.md, preprocessor.py, tov_spec.md,
subagent_phase1.md, subagent_phase2.md, brigadir.md,
layer2_widget.md, widget_generator.py, md_parser.py,
**layer3_recon.md + recon_patch_template.py (Слой 3)**,
widget_skeleton.html, validate_widget.py, widget.md, start_form.html,
5 профилей (kartograf_profile_*.md), tests/.

**Правила, которые стабилизировались в этой сессии:**
- Жирное = сжатая формулировка ключевой мысли (тест канвы), структурные пометки в счёт не идут.
- Логическая реконструкция в мастер-MD = технический минимум (цитата + таблица), без прозы.
- Метки (И)/(М)/(Д) в Карте — внутренний инструмент агента, в финальный файл не идут.
- Слой 3 левая колонка: взгляд сверху для читателя, простым языком, без литературщины.

**В бэклоге:**
- 4 оптимизации скилла из [docs/konspekt-optimization-2026-05-23.md](konspekt-optimization-2026-05-23.md): порядок C → A → D → B.
- Mermaid-диаграмма (Item 3, отдельная сессия).
- Снять заморозку с Траектории или переориентировать виджет на Логическую реконструкцию.
- Формат `.md` для карты смыслов (TODO в `CLAUDE.md` проекта).
- Накопить ещё 1–2 SVG-формы для Слоя 3 и обновить таблицу типовых форм.

**Следующий шаг:** наведение порядка в `docs/` (uncommitted-файлы накопились), потом сессия оптимизации (пункт C — серийный контекст выборочно — самый большой токен-эффект), потом — тестовый прогон оптимизированного /konspekt.

---

## Хронология

| Дата | Что | Почему |
|---|---|---|
| До мая 2026 | 3 шага: Карта→Конспект→Виджет | Транскрипт ×2, форматы в логике |
| 2026-05-04 | Слой 1: мастер-MD | Разделили вычисление и представление |
| 2026-05-05 утро | Двухфазный, чанки, бригадир | Провал на реальном транскрипте |
| 2026-05-05 вечер | Слой 2: виджет, форматирование | Готовый MD → HTML, текст стал читаемым |
| 2026-05-14 | Единое форматирование прозы + Траектория | Бэклог Item 1 + Item 2 |
| 2026-05-15 утро | Нарезка по времени + унификация пайплайна | Чанки слишком большие; бригадир перегружен |
| 2026-05-15 вечер | Корневая правка Фазы 2: транскрипт → Текст → Карта | Субагент писал кратко — неверный фрейм |
| 2026-05-15 ночь | Самопроверки бригадира #1, #2, #4 + Принцип абзаца | Качество на ключевых этапах без церемонии |
| 2026-05-16 | Фаза 2: отрезок сегмента вместо целого чанка | ~58% экономии input без потери скорости/качества |
| 2026-05-17 | 10 пунктов оптимизации: Sonnet на Ф1/Ф2, встраивание спек, гибридный ToV, заморозка Траектории, методичка укрупнения, уровни (И)/(М)/(Д) | Прогон ч2 курса съел 5-часовой rate-limit; брейншторм превратил точечный фикс в радикальную переработку 57–64% |
| 2026-05-19 | Парсер мастер-MD: `widget_generator.py master.md → HTML` напрямую; JSON-режим резервный | Ручная сборка JSON ~15 мин/виджет с риском пропусков; MD = единственный источник правды |
| 2026-05-20 | Главная мысль ДО сегментации: ШАГ 1 разбит на 1.1/1.2/1.3, в шапку добавлена строка `**Главная мысль:**` | Сегменты «ради сегментов» — нарезка без зафиксированной риторической оси давала разрозненные блоки |
| 2026-05-21 | Прототип Слоя 3 — кастомный экран реконструкции виджета: проза короткими абзацами + SVG-схема слева, «артефакты урока» с раскрывающимися промптами и кликабельными `↗` справа. Два patch-скрипта на М2-У6/У7 как референсы | Из коробки экран реконструкции — кирпич прозы слева + пустая колонка справа. Не работает как «мгновенная картина видео». Handoff на вшивание в скилл — `docs/konspekt-layer3-handoff.md` |
| 2026-05-21 | Слой 3 вшит в скилл: `layer3_recon.md` (инструкция для агента) + `recon_patch_template.py` (шаблон с охранным запуском через подсчёт `# TODO`). `SKILL.md` упоминает Слой 3 | Одноразовые patch-скрипты М2-У6/У7 — не переиспользуемый компонент. Handoff потребовал выделить универсальную часть отдельно |
| 2026-05-21 (вечер) | Применение Слоя 3 к М1-У5-2 (Документация как память проекта) из архива. `patch_recon_m1u5-2.py` по шаблону `recon_patch_template.py`; новая SVG-форма «проблема → 3 файла-памяти → результат + цикл правок» | Третий виджет с кастомным экраном после М2-У6/У7. Подтвердило, что шаблон Слоя 3 работает «как пользователь» и виджеты из `transcripts/archive/` обрабатываются той же командой |
| 2026-05-23 | Правило жирного: с количественного («1–2 якоря, 1–3 слова») на качественное (сжатая формулировка ключевой мысли + тест канвы). Технический минимум Логической реконструкции в мастер-MD. Метки (И)/(М)/(Д) — внутренний инструмент. Виджет М2-У10-1 со Слоем 3. Слой 3: «вокруг узлов дуги» + «простым языком, как объясняешь другу» (после трёх правок реконструкции). Memory `feedback_plain_language`. План оптимизации скилла для отдельной сессии | Количественное правило ломалось на смысле — выделяли слова-метки, не складывались в канву. Слой 3 после удаления прозы из мастер-MD рисковал деградировать в плоский пересказ. Реконструкцию я сначала написал пересказом, потом литературоведческим разбором — пользователь дважды поправил, до простого языка |
