# План реализации: блок «Замысел и маршрут»

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Перенести источник правды о маршруте урока из Слоя 3 в мастер-MD: парсер распознаёт новый раздел «## Замысел и маршрут», методология ШАГ 1 строит маршрут до сегментации, Слой 3 верстает готовый блок вместо сочинения.

**Architecture:** Раздел условный (только при наличии единой нити в материале). Парсер отдаёт два поля: `route_prose` и `route_structure`. Парсер не различает форму опорной структуры (этапы / тезисы / опорные точки) — отдаёт сырой список. Слой 3 бимодален: блок есть → верстает; нет → старая логика сочинения (для совместимости со старыми мастер-MD).

**Tech Stack:** Python 3 + pytest для парсера; правки текстовых инструкций для агента в `.md` файлах скилла.

**Спека:** [2026-05-25-konspekt-route-block-design.md](../specs/2026-05-25-konspekt-route-block-design.md)

---

## Файлы

- **Modify:** `.claude/skills/konspekt/md_parser.py` — распознавание нового раздела, поля `route_prose` / `route_structure` в data.
- **Create:** `.claude/skills/konspekt/tests/test_route_block.py` — pytest-тесты парсера.
- **Modify:** `.claude/skills/konspekt/SKILL.md` — новый под-этап 1.3, переименование 1.3→1.4, новый под-этап в конце ШАГ 2, добавление шаблона блока в мастер-MD.
- **Modify:** `.claude/skills/konspekt/profile_lecture.md` — ссылочная строка на новый под-этап 1.3.
- **Modify:** `.claude/skills/konspekt/layer3_recon.md` — бимодальность левой колонки (верстать готовое vs сочинять).

---

## Task 1: Парсер — распознать раздел без падения

Сейчас `_split_sections` бросает `MasterMDParseError` на любом разделе верхнего уровня, кроме `# `, `## Логическая реконструкция` и `## Сегмент `. Нужно научить его принимать `## Замысел и маршрут` и складывать в отдельное поле.

**Files:**
- Modify: `.claude/skills/konspekt/md_parser.py:83-115`
- Create: `.claude/skills/konspekt/tests/test_route_block.py`

- [ ] **Step 1: Написать падающий тест на распознавание раздела**

Создать файл `.claude/skills/konspekt/tests/test_route_block.py`:

```python
import os
import sys

import pytest

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))
from md_parser import parse_master_md, MasterMDParseError


def _write_master(tmp_path, body):
    """Записывает body в tmp/master.md и возвращает путь."""
    p = tmp_path / "master.md"
    p.write_text(body, encoding='utf-8')
    return str(p)


MINIMAL_SEGMENT = """
## Сегмент 1 | 00:00:00–00:05:00 | Введение

**Тип:** Введение
**Ключевая мысль:** Запускаем разбор.

### Карта

- **(И)** Открытие темы.

### Текст

Спикер вводит тему.
"""


def test_parser_accepts_route_block(tmp_path):
    """Раздел `## Замысел и маршрут` должен парситься без ошибки."""
    body = """# Тест

**Спикер:** Тестовый

---

## Замысел и маршрут

Автор разбирает тему пошагово.

**Этапы:**

1. Этап 1 — открыть тему.
2. Этап 2 — углубить.

---
""" + MINIMAL_SEGMENT
    path = _write_master(tmp_path, body)
    data = parse_master_md(path)  # не должно бросить
    assert 'route' in data
```

- [ ] **Step 2: Запустить тест, убедиться что он падает**

Run: `cd .claude/skills/konspekt && python -m pytest tests/test_route_block.py::test_parser_accepts_route_block -v`
Expected: FAIL с `MasterMDParseError: Неизвестный раздел верхнего уровня: '## Замысел и маршрут'`

- [ ] **Step 3: Расширить `_split_sections` для распознавания раздела**

В `.claude/skills/konspekt/md_parser.py` найти функцию `_split_sections` (строки 83-115). Заменить её на:

```python
def _split_sections(text):
    """Режет мастер-MD на header, route (опц.), reconstruction (опц.), segments[]."""
    text = text.replace('\r\n', '\n').replace('\r', '\n')

    parts = re.split(r'\n---\n', text)
    parts = [p.strip() for p in parts if p.strip()]

    if not parts:
        raise MasterMDParseError("Пустой файл")

    header = parts[0]
    if not header.startswith('# '):
        raise MasterMDParseError(f"Первый блок не начинается с `# Название`, найдено: {header[:60]!r}")

    route = None
    reconstruction = None
    segments = []
    for part in parts[1:]:
        if part.startswith('## Замысел и маршрут'):
            route = part
        elif part.startswith('## Логическая реконструкция'):
            reconstruction = part
        elif part.startswith('## Сегмент '):
            segments.append(part)
        else:
            raise MasterMDParseError(
                f"Неизвестный раздел верхнего уровня: {part.splitlines()[0]!r}"
            )

    if not segments:
        raise MasterMDParseError("Не найдено ни одного `## Сегмент N | ...`")

    return {'header': header, 'route': route, 'reconstruction': reconstruction, 'segments': segments}
```

- [ ] **Step 4: Расширить `parse_master_md`, чтобы пробрасывать `route` в результат**

В той же функции `parse_master_md` (строки 65-79). Между присвоением `reconstruction = ...` и `segments = ...` (строка 70) добавить и обновить возврат:

```python
def parse_master_md(path):
    """Главная функция: читает файл, возвращает dict для build_html."""
    text = Path(path).read_text(encoding='utf-8')
    sections = _split_sections(text)
    meta = _parse_meta(sections['header'], path)
    route = _parse_route(sections['route']) if sections['route'] else None
    reconstruction = _parse_reconstruction(sections['reconstruction']) if sections['reconstruction'] else None
    prompt_counter = [0]
    segments = [_parse_segment(b, i + 1, prompt_counter) for i, b in enumerate(sections['segments'])]
    return {
        'meta': meta,
        'route': route,
        'reconstruction': reconstruction,
        'trajectory': None,
        'segments': segments,
        'prompts': {},
    }
```

- [ ] **Step 5: Добавить заглушку `_parse_route` (полная реализация — в Task 2)**

В `.claude/skills/konspekt/md_parser.py` сразу перед функцией `_parse_reconstruction` (строка 161) вставить:

```python
def _parse_route(block):
    """`## Замысел и маршрут\n\n...` -> {'prose': <html>, 'structure': <html>}.

    Полная реализация в Task 2. Пока возвращает заглушку, чтобы тесты Task 1 проходили.
    """
    return {'prose': '', 'structure': ''}
```

- [ ] **Step 6: Запустить тест, убедиться что он проходит**

Run: `cd .claude/skills/konspekt && python -m pytest tests/test_route_block.py::test_parser_accepts_route_block -v`
Expected: PASS

- [ ] **Step 7: Написать тест на отсутствие блока**

В `tests/test_route_block.py` добавить:

```python
def test_parser_route_is_none_when_block_missing(tmp_path):
    """Мастер-MD без блока: data['route'] is None, парсер не падает."""
    body = """# Тест

**Спикер:** Тестовый

---
""" + MINIMAL_SEGMENT
    path = _write_master(tmp_path, body)
    data = parse_master_md(path)
    assert data['route'] is None
```

- [ ] **Step 8: Запустить, должен проходить сразу**

Run: `cd .claude/skills/konspekt && python -m pytest tests/test_route_block.py -v`
Expected: оба теста PASS

- [ ] **Step 9: Коммит**

```bash
git add .claude/skills/konspekt/md_parser.py .claude/skills/konspekt/tests/test_route_block.py
git commit -m "feat(parser): распознавание раздела '## Замысел и маршрут' (заглушка _parse_route)"
```

---

## Task 2: Парсер — разделение прозы и опорной структуры

Превратить заглушку `_parse_route` в работающий парсер. Делит блок на прозу (всё до первой строки-маркера `**Label:**`) и опорную структуру (всё после). Маркер ищется как любая строка целиком `**...:**` (без слов после двоеточия) — это даёт гибкость для разных форм блока (этапы / тезисы / опорные точки).

**Files:**
- Modify: `.claude/skills/konspekt/md_parser.py` — функция `_parse_route`.
- Modify: `.claude/skills/konspekt/tests/test_route_block.py` — добавить тесты.

- [ ] **Step 1: Написать падающий тест на разделение прозы и структуры**

В `tests/test_route_block.py` добавить:

```python
def test_route_block_parses_prose_and_structure(tmp_path):
    """Проза до `**Этапы:**` идёт в route_prose, нумерованный список — в structure."""
    body = """# Тест

**Спикер:** Тестовый

---

## Замысел и маршрут

Автор разбирает тему **пошагово**.

Это второй абзац прозы.

**Этапы:**

1. Этап 1 — открыть тему.
2. Этап 2 — углубить.

---
""" + MINIMAL_SEGMENT
    path = _write_master(tmp_path, body)
    data = parse_master_md(path)
    route = data['route']

    # Проза: два абзаца, жирное конвертировано в <strong>
    assert '<p>Автор разбирает тему <strong>пошагово</strong>.</p>' in route['prose']
    assert '<p>Это второй абзац прозы.</p>' in route['prose']
    # В прозе не должно быть текста этапов
    assert 'Этап 1' not in route['prose']

    # Структура: нумерованный список
    assert '<ol' in route['structure']
    assert '<li>Этап 1 — открыть тему.</li>' in route['structure']
    assert '<li>Этап 2 — углубить.</li>' in route['structure']
```

- [ ] **Step 2: Запустить тест, убедиться что он падает**

Run: `cd .claude/skills/konspekt && python -m pytest tests/test_route_block.py::test_route_block_parses_prose_and_structure -v`
Expected: FAIL — заглушка возвращает пустые строки.

- [ ] **Step 3: Реализовать `_parse_route`**

Заменить заглушку `_parse_route` в `md_parser.py` на:

```python
def _parse_route(block):
    """`## Замысел и маршрут\n\n<prose>\n\n**<Label>:**\n\n<list>` -> {'prose', 'structure'}.

    Маркер опорной структуры — любая строка целиком вида `**Label:**` (без текста после).
    Парсер не интерпретирует форму списка — отдаёт как нумерованный/маркированный список HTML.
    Если маркера нет — весь блок считается прозой, structure == ''.
    """
    body = re.sub(r'^##\s+Замысел и маршрут\s*\n', '', block, count=1)
    lines = body.split('\n')

    # Находим строку-маркер: `**...:**` на своей строке (после strip — ровно эта запись).
    marker_re = re.compile(r'^\*\*[^*\n]+:\*\*\s*$')
    split_idx = None
    for idx, line in enumerate(lines):
        if marker_re.match(line.strip()):
            split_idx = idx
            break

    if split_idx is None:
        prose_lines = lines
        structure_lines = []
    else:
        prose_lines = lines[:split_idx]
        structure_lines = lines[split_idx + 1:]

    prose_html = _render_paragraphs(prose_lines)
    structure_html = _render_list(structure_lines)

    return {'prose': prose_html, 'structure': structure_html}


def _render_paragraphs(lines):
    """Список строк (с пустыми разделителями) -> склейка `<p>...</p>` через _apply_inline."""
    paragraphs = []
    buf = []
    for line in lines:
        if line.strip() == '':
            if buf:
                paragraphs.append(_apply_inline(' '.join(buf).strip()))
                buf = []
        else:
            buf.append(line.strip())
    if buf:
        paragraphs.append(_apply_inline(' '.join(buf).strip()))
    return ''.join(f'<p>{p}</p>' for p in paragraphs)


def _render_list(lines):
    """Строки после маркера -> `<ol>` если нумерованный, `<ul>` если маркированный, иначе абзацы."""
    items = [l.strip() for l in lines if l.strip()]
    if not items:
        return ''

    if all(re.match(r'\d+\.\s+', it) for it in items):
        body = ''.join(
            f'<li>{_apply_inline(re.match(r"\d+\.\s+(.*)", it).group(1))}</li>'
            for it in items
        )
        return f'<ol style="margin:6px 0 0 18px">{body}</ol>'
    if all(it.startswith('- ') for it in items):
        body = ''.join(f'<li>{_apply_inline(it[2:])}</li>' for it in items)
        return f'<ul style="margin:6px 0 0 18px">{body}</ul>'

    # Смесь форматов или просто абзацы — рендерим как абзацы для устойчивости.
    return _render_paragraphs(lines)
```

- [ ] **Step 4: Запустить тест, убедиться что он проходит**

Run: `cd .claude/skills/konspekt && python -m pytest tests/test_route_block.py::test_route_block_parses_prose_and_structure -v`
Expected: PASS

- [ ] **Step 5: Тест на маркированный список**

В `tests/test_route_block.py` добавить:

```python
def test_route_block_structure_bullets(tmp_path):
    """Маркированный список после маркера рендерится как <ul>."""
    body = """# Тест

**Спикер:** Тестовый

---

## Замысел и маршрут

Лекция о ключевых тезисах.

**Тезисы:**

- Первый тезис.
- Второй тезис.

---
""" + MINIMAL_SEGMENT
    path = _write_master(tmp_path, body)
    data = parse_master_md(path)
    structure = data['route']['structure']
    assert '<ul' in structure
    assert '<li>Первый тезис.</li>' in structure
    assert '<li>Второй тезис.</li>' in structure
```

- [ ] **Step 6: Запустить, должен проходить**

Run: `cd .claude/skills/konspekt && python -m pytest tests/test_route_block.py::test_route_block_structure_bullets -v`
Expected: PASS

- [ ] **Step 7: Тест на блок без опорной структуры (одна проза)**

В `tests/test_route_block.py` добавить:

```python
def test_route_block_only_prose(tmp_path):
    """Если маркера `**...:**` нет — весь блок идёт в prose, structure пуст."""
    body = """# Тест

**Спикер:** Тестовый

---

## Замысел и маршрут

Просто проза без опорной структуры — короткий разбор.

---
""" + MINIMAL_SEGMENT
    path = _write_master(tmp_path, body)
    route = parse_master_md(path)['route']
    assert '<p>Просто проза без опорной структуры — короткий разбор.</p>' == route['prose']
    assert route['structure'] == ''
```

- [ ] **Step 8: Запустить весь набор тестов**

Run: `cd .claude/skills/konspekt && python -m pytest tests/test_route_block.py -v`
Expected: все 4 теста PASS.

- [ ] **Step 9: Запустить весь набор тестов скилла (регрессия)**

Run: `cd .claude/skills/konspekt && python -m pytest tests/ -v`
Expected: все тесты PASS, существующие `test_preprocessor.py` и `test_widget_generator.py` не сломались.

- [ ] **Step 10: Коммит**

```bash
git add .claude/skills/konspekt/md_parser.py .claude/skills/konspekt/tests/test_route_block.py
git commit -m "feat(parser): разбор блока '## Замысел и маршрут' на прозу и опорную структуру"
```

---

## Task 3: SKILL.md — методология ШАГ 1 и ШАГ 2

Переписать ШАГ 1: добавить новый под-этап 1.3 «Замысел и маршрут», переименовать существующий 1.3 в 1.4, обновить логику сегментации. В ШАГ 2 добавить под-этап «Дописать прозу взгляда сверху». Включить блок в шаблон мастер-MD.

**Files:**
- Modify: `.claude/skills/konspekt/SKILL.md:75-131` (ШАГ 1).
- Modify: `.claude/skills/konspekt/SKILL.md` — добавить раздел в ШАГ 2 после `### Логическая реконструкция (технический минимум)`.

- [ ] **Step 1: Перечитать текущий ШАГ 1**

Read: `.claude/skills/konspekt/SKILL.md` строки 69-131.

- [ ] **Step 2: Добавить новый под-этап 1.3 «Замысел и маршрут»**

В `SKILL.md` найти строку `### 1.3. Сегментация` (строка 90). Перед ней вставить:

```markdown
### 1.3. Замысел и маршрут (условно)

До сегментации проверить, есть ли у материала **единая нить** — методология / развивающаяся мысль / разбор с аркой.

**Тест на нить:**
> Можешь сформулировать в 4–8 пунктах, что автор делает по порядку с начала до конца? Если да — нить есть, заводи блок. Если получаются разрозненные темы без преемственности — нити нет.

**Если нити нет** — этот под-этап пропускается, идти сразу к 1.4.

**Если нить есть** — сформулировать **опорную структуру** автора: 4–8 пунктов. Форма зависит от типа материала:

- методология → нумерованная лестница этапов: «Этап N — <глагол>: <суть> — почему отсюда / что даёт»;
- лекция-идея → список ключевых тезисов / поворотов мысли;
- разбор с аркой → опорные точки сюжета или акценты.

**Прозу «взгляд сверху» здесь НЕ пишем** — только опорную структуру. Проза дописывается в конце ШАГ 2, когда уже видны финальные сегменты.

Результат под-этапа — заготовка раздела `## Замысел и маршрут` в мастер-MD: только заголовок, маркер опорной структуры (`**Этапы:**`, `**Тезисы:**`, `**Опорные точки:**` — по форме) и список. Прозы пока нет.

**Примеры теста на нить:**
- **Есть нить** (методология): лекция «как создать персонального ассистента» — этапы 1) собрать промпт, 2) добавить контекст, 3) проверить ответы, 4) дописать ошибки. Последовательность видна.
- **Нет нити** (разговорный подкаст): обсуждают три не связанные новости + Q&A. Каждая тема самостоятельна.

```

- [ ] **Step 3: Переименовать существующий 1.3 в 1.4 и расширить логику сегментации**

В `SKILL.md` (строки 90-100 на момент написания, после вставки выше — сдвинутся) найти заголовок `### 1.3. Сегментация` и тело раздела. Заменить заголовок и первый абзац:

Старое:
```markdown
### 1.3. Сегментация

Построить навигационную карту:

...

**Сегментация — от главной мысли.** Каждый сегмент должен раскрывать какую-то грань главной мысли (1.2). Если сегмент на неё не работает — это либо служебный фрагмент (отбросить или объединить с соседним), либо главная мысль сформулирована слишком узко (вернуться к 1.2).
```

Новое:
```markdown
### 1.4. Сегментация

Построить навигационную карту:

...

**Сегментация — от опорной структуры, если она есть (1.3).** Каждый сегмент = один пункт опорной структуры или его явная часть. Заголовки сегментов наследуют формулировки пунктов (для лестницы этапов — дословно; для тезисов / опорных точек — производные).

**Если опорной структуры нет** — сегментация от главной мысли (1.2): каждый сегмент раскрывает какую-то грань главной мысли. Если сегмент на неё не работает — это либо служебный фрагмент (отбросить или объединить с соседним), либо главная мысль сформулирована слишком узко (вернуться к 1.2).
```

Остальное тело раздела (Что такое сегмент / Маркеры границы / Анти-маркеры / Тест на укрупнение) — без изменений.

- [ ] **Step 4: Добавить в ШАГ 2 под-этап «Дописать прозу взгляда сверху»**

В `SKILL.md` найти раздел `### Логическая реконструкция (технический минимум)` (строка 300). **Перед** этим разделом вставить новый:

```markdown
### Дописать прозу «взгляд сверху» (если блок 1.3 был открыт)

Выполняется в самом конце ШАГ 2, после того как **все сегменты написаны**.

Вернуться к началу мастер-MD, в раздел `## Замысел и маршрут`, и **дописать 4–6 коротких абзацев прозы** перед опорной структурой. К этому моменту перед глазами уже все заголовки сегментов, тексты, главная мысль — проза пишется ретроспективно, с полной картиной.

**Содержание прозы:**
- О чём этот урок (1 абзац).
- Что автор хочет донести / какая риторическая позиция (1 абзац).
- Какая внутри логика, как этапы связаны (1–2 абзаца).
- Есть ли поворот / акцент / что не очевидно (опционально, 1 абзац).

**Простой язык.** Без «риторической дуги», «глубинного зачем», «фундамента, который держит». Тест — прочитать вслух: звучит как нормальная речь?

**Жирное = сжатая мысль абзаца.** Не выделять цвет/смысл вообще — только ключевую формулировку, чтобы при сканировании сверху вниз глаз цеплялся за неё.

**Опорную структуру при необходимости подправить:** если за время написания сегментов формулировки этапов уточнились, привести список пунктов в соответствие с финальными заголовками сегментов.

Если блок 1.3 **не открывался** (нет нити) — этот под-этап пропускается.

```

- [ ] **Step 5: Добавить шаблон блока в раздел «Шапка файла»**

В `SKILL.md` найти раздел `### Шапка файла` (строка 320). Дополнить его указанием, что блок `## Замысел и маршрут` идёт **сразу после шапки, до Логической реконструкции и сегментов**. После существующего шаблона шапки добавить:

```markdown
**Шаблон раздела «Замысел и маршрут» (вставляется сразу после шапки, если блок 1.3 был открыт):**

```markdown
## Замысел и маршрут

[4–6 коротких абзацев прозы «взгляд сверху» — дописываются в конце ШАГ 2.
До этого момента раздел содержит только заголовок и опорную структуру.]

**Этапы:**

1. **Этап 1 — <глагол>: <суть>** — почему отсюда начинаем / что даёт.
2. **Этап 2 — <глагол>: <суть>** — почему за предыдущим следует это.
   ...
```

Имя маркера опорной структуры зависит от формы:
- `**Этапы:**` — методология (нумерованная лестница);
- `**Тезисы:**` — лекция-идея (ключевые мысли);
- `**Опорные точки:**` — разбор с аркой.

Парсер `md_parser.py` ищет любой подзаголовок вида `**...:**` на отдельной строке — имя маркера на разбор не влияет.

```

- [ ] **Step 6: Прочитать обновлённый файл, проверить связность**

Read: `.claude/skills/konspekt/SKILL.md` строки 69-160 — должно читаться без обрывов и противоречий.

- [ ] **Step 7: Коммит**

```bash
git add .claude/skills/konspekt/SKILL.md
git commit -m "feat(skill): ШАГ 1.3 'Замысел и маршрут' + дописывание прозы в конце ШАГ 2"
```

---

## Task 4: profile_lecture.md — ссылка на 1.3

Профиль уже содержит ключевой принцип «лекции с единым архитектурным замыслом — сегменты = ШАГИ единого процесса». Нужна одна ссылочная строка, чтобы агент после чтения профиля знал про новый под-этап 1.3.

**Files:**
- Modify: `.claude/skills/konspekt/profile_lecture.md:10-29`

- [ ] **Step 1: Прочитать секцию «Лекции с единым архитектурным замыслом»**

Read: `.claude/skills/konspekt/profile_lecture.md` строки 10-29.

- [ ] **Step 2: Дописать ссылочную строку**

В конце секции (после строки 28 `маршрут метода и формулировки этапов — в `tov_<серия>.md`, сверяться с ним.`) добавить:

```markdown
- **Маршрут до сегментации:** до нарезки на сегменты формируется раздел
  `## Замысел и маршрут` (см. `SKILL.md` ШАГ 1.3). Опорная структура из этого
  блока диктует границы и заголовки сегментов в 1.4.
```

- [ ] **Step 3: Коммит**

```bash
git add .claude/skills/konspekt/profile_lecture.md
git commit -m "docs(profile): ссылка на ШАГ 1.3 'Замысел и маршрут'"
```

---

## Task 5: layer3_recon.md — бимодальность левой колонки

Слой 3 перестаёт сочинять прозу «взгляд сверху», когда блок `## Замысел и маршрут` есть в мастер-MD: берёт `data['route']['prose']` и `data['route']['structure']` и верстает в левую колонку. Если блока нет — старая логика (сочинение из заголовков сегментов) остаётся как fallback.

SVG-схема и правая колонка артефактов не трогаются.

**Files:**
- Modify: `.claude/skills/konspekt/layer3_recon.md:1-100` — раздел про левую колонку.

- [ ] **Step 1: Прочитать текущий раздел про левую колонку**

Read: `.claude/skills/konspekt/layer3_recon.md` строки 1-110.

- [ ] **Step 2: Добавить в начало файла раздел про источник правды**

В `layer3_recon.md` сразу после первого заголовка раздела (там, где описано назначение Слоя 3) добавить новый раздел:

```markdown
## Источник правды о маршруте

С 2026-05 левая колонка («взгляд сверху») имеет **два режима** работы:

**Режим А — блок «## Замысел и маршрут» есть в мастер-MD** (новый поток).

`md_parser.parse_master_md(...)` возвращает `data['route'] = {'prose': <html>, 'structure': <html>}`. Слой 3:
- **не сочиняет** прозу заново — копирует `data['route']['prose']` в левую колонку как есть (только верстальное оформление);
- **не переименовывает** опорную структуру — копирует `data['route']['structure']` как есть;
- может подправить только цвета / отступы / шрифт — смысл не интерпретирует.

Если в мастер-MD написано «Этап 3 — настроить рабочее место», в виджете тоже «Этап 3 — настроить рабочее место». Слово в слово.

**Режим Б — блока нет** (`data['route'] is None`, старые мастер-MD).

Слой 3 работает как раньше: сочиняет прозу из заголовков сегментов и Логической реконструкции. Это fallback для совместимости.

**Как определить режим:** `if data.get('route'): режим А, иначе режим Б`.

```

- [ ] **Step 3: Найти раздел `### Левая колонка` (строка 32 в текущем файле)**

В `layer3_recon.md` найти заголовок `### Левая колонка` и тело раздела (строки 32-89 в текущем файле, после вставки в Step 2 номера сдвинутся).

В первый абзац раздела (после строки `**Цель левой колонки — дать пользователю взгляд на урок сверху.**`) добавить отсылку к режимам:

```markdown
> **Источник содержимого:** см. раздел «Источник правды о маршруте» выше.
> В режиме А содержимое колонки = `data['route']['prose']` + `data['route']['structure']` (без изменений).
> Инструкции ниже — для режима Б (когда блока нет).
```

- [ ] **Step 4: Прочитать обновлённый файл целиком**

Read: `.claude/skills/konspekt/layer3_recon.md` — проверить, что новый раздел и отсылка читаются согласованно с остальным.

- [ ] **Step 5: Коммит**

```bash
git add .claude/skills/konspekt/layer3_recon.md
git commit -m "feat(layer3): бимодальная левая колонка — режим А (блок есть) + режим Б (fallback)"
```

---

## Task 6: Архивировать черновик-брейнсторм

`docs/design_route_block.md` — это черновик-брейнсторм, который теперь замещён спекой. Оставляем файл (для git-history и отслеживаемости), но добавляем шапку «замещён».

**Files:**
- Modify: `docs/design_route_block.md` — добавить пометку в начале.

- [ ] **Step 1: Добавить шапку «замещён» в `docs/design_route_block.md`**

В начало файла (перед строкой `# Дизайн: «Карта замысла» в мастер-MD как основа Слоя 3`) вставить:

```markdown
> **Статус: замещён.** Финальная спека — [docs/superpowers/specs/2026-05-25-konspekt-route-block-design.md](superpowers/specs/2026-05-25-konspekt-route-block-design.md).
> Этот файл оставлен как история обсуждения 2026-05-25.

```

- [ ] **Step 2: Коммит**

```bash
git add docs/design_route_block.md
git commit -m "docs: пометить черновик design_route_block.md как замещённый спекой"
```

---

## Task 7: Контрольный прогон скилла (ручная проверка)

Запустить `/konspekt` на свежем транскрипте лекции-методологии (любой урок, который раньше шёл «вразнобой по заголовкам»). Проверить, что:
1. Агент на ШАГ 1.3 формулирует опорную структуру до сегментации.
2. Заголовки сегментов в результате имеют преемственность (читаются подряд как маршрут).
3. В конце ШАГ 2 агент дописывает прозу в блок «## Замысел и маршрут».
4. Парсер не падает на новом мастер-MD.

**Это не автоматизированный тест — это критерий приёма для пользователя.**

- [ ] **Step 1: Запустить `/konspekt` на свежем транскрипте**

Не код-шаг — ручное действие пользователя. Если результат заголовков по-прежнему неудовлетворителен, вернуться к спеке и уточнить формулировку Задачи 3 (под-этап 1.3) или Задачи 4.

- [ ] **Step 2 (после успешного прогона): Обновить хранилище**

Записать в `vibecoding vault/projects/konspekt-project.md` факт релиза блока «Замысел и маршрут» и наблюдения с первого прогона.

---

## Самопроверка плана (выполнено автором перед сдачей)

- **Покрытие спеки:**
  - Раздел 1 (Условность блока) → Task 3, Step 2 (тест на нить и примеры).
  - Раздел 2 (Контракт парсера) → Tasks 1, 2 (распознавание + разбор + bimodal None).
  - Раздел 3 (Слой 3) → Task 5 (бимодальность, запрет сочинения в режиме А).
  - Раздел 4 (Методология ШАГ 1) → Task 3 (под-этапы 1.3, 1.4 и ШАГ 2).
  - Раздел 5 (Миграция) → Task 5, Step 2 (режим Б как fallback). Дополнительной задачи не требуется — старые файлы не трогаем.

- **Плейсхолдеров нет.** Везде указаны конкретные строки кода, конкретные пути файлов, конкретные тексты вставок.

- **Согласованность типов.** Поле `route` в `data` — `dict` с ключами `'prose'` и `'structure'` (оба `str` с HTML), либо `None`. Используется единообразно во всех задачах.

- **Открытый вопрос спеки про имя маркера** разрешён прагматично: парсер принимает любой `**Label:**`, шаблон в SKILL.md рекомендует `**Этапы:**` / `**Тезисы:**` / `**Опорные точки:**` по форме. Не блокирует реализацию.
