# Python
__pycache__/
*.pyc

# Продукты скилла (конспекты/обзоры/виджеты) вынесены из репо в воркспейс
# D:\Users\Вова\Desktop\Work\VibeCoding\K_T_P\ — в репо не коммитятся.
# Страховка на случай запуска /konspekt из самого репо вместо K_T_P:
previews/
OUT_*

# Одноразовые patch-скрипты Слоя 3 — артефакты конкретных лекций, не код скилла.
# Шаблон для копирования: .claude/skills/konspekt/recon_patch_template.py
# Живут в _patches/, самоуничтожаются через 7 дней после создания.
patch_recon_*.py
_patches/*
!_patches/.gitkeep

# Playwright MCP — снимки и логи браузера, пишутся плагином в cwd
.playwright-mcp/

# Claude Code — рабочие артефакты (git worktree'ы ответвлений, локи планировщика)
.claude/worktrees/
.claude/scheduled_tasks.lock

# OS
.DS_Store
Thumbs.db

# Сессионные черновики (Codex-ревью, временные документы)
.tmp/

# Корпус дообучения заголовков: рабочие дайджесты с выдержками транскриптов.
# Скрипты-инструменты лежат в tools/corpus/ (под git), сами дайджесты — нет.
.corpus-scratch/
