---
name: konspekt
description: Обрабатывает транскрипт видеовыступления и создаёт мастер-MD. Чанки по времени (≤ 60 мин каждый), фоллбэк на токены при отсутствии тайминга. Двухфазный пайплайн с параллельными субагентами. Форматы вывода (Слой 2) — отдельный пайплайн.
---

# Скилл: Обработка транскрипта выступления

## Файлы скилла

- `preprocessor.py` — статистика, нарезка транскрипта (ШАГ 0), отрезка чанка по границам сегмента (Фаза 2)
- `tov_spec.md` — ToV-спецификация (базовые правила + динамический блок)
- `audience_spec.md` — описание целевой Аудитории (единый адресат для всех агентов)
- `subagent_phase1.md` — инструкции для субагентов Фазы 1 (черновая сегментация, роль методолога)
- `subagent_phase2.md` — инструкции для субагентов Фазы 2 (написание текста, роль спикера + читательская самопроверка)
- `brigadir.md` — глобальный обзор, верификация, гармонизация, переходы, реконструкция, траектория, сборка мастер-MD, финальная самопроверка
- `kartograf_profile_*.md` — профили контента (справочник; детальная переработка — отдельный спек)
- `layer2_widget.md` — виджет (Слой 2, конвертация мастер-MD → JSON → HTML)

---

## Пайплайн

```
Транскрипт
    ↓
ШАГ 0: Оценка длительности (preprocessor.py info)
    ↓
    ├─ если ≤ 60 мин ──────────────────────────────────→ один чанк (транскрипт целиком)
    │
    └─ если > 60 мин
                ↓
          Нарезка на чанки (~60 мин каждый)
    ↓
ФАЗА 1: Субагенты — черновая сегментация (1 или N параллельно)
    ↓
БРИГАДИР: Глобальный обзор → чистовая карта
    ↓
ФАЗА 2: Субагенты — написание текста (по одному на сегмент, параллельно)
    ↓
БРИГАДИР: Верификация + гармонизация + переходы + реконструкция + сборка
    ↓
Мастер-MD → transcripts/
```

---

## ШАГ 0: Запуск

### Сбор контекста

Выполнить автоматически, без вопросов к пользователю:

1. Проверить `transcripts/` на наличие `*_мастер.md` — для серийного контекста.
   Если найдены — прочитать и сообщить одной строкой.

2. Запустить:
   ```
   python .claude/skills/konspekt/preprocessor.py info [путь к транскрипту]
   ```
   Получить оценку длительности (или токенов, если тайминга нет).

Затем задать пользователю **одним сообщением**:

```
1. Имя спикера (если не указано в транскрипте)
2. Профиль контента:
   - [ ] Лекция
   - [ ] Custdev-интервью
   - [ ] Встреча / синк
   - [ ] Конференция / конференс-доклад
   Если не уверен — Claude определит автоматически по транскрипту.
3. Режим работы:
   - Автоматический (без промежуточных проверок)
   - С проверкой (показывать чистовую карту перед Фазой 2; показать мастер-MD в конце)
```

Начинать работу только после получения ответов.

### Выбор пути

| Длительность | Действие |
| --- | --- |
| ≤ 60 мин (или ≤ 12K токенов без тайминга) | Один чанк — транскрипт целиком, без нарезки |
| > 60 мин (или > 12K токенов без тайминга) | Нарезка — `preprocessor.py windows` + `split` |

---

## ШАГ 1: Подготовка чанков

**Если один чанк:** нарезка не нужна. Транскрипт целиком используется как единственный чанк в Фазе 1.

**Если нарезка нужна:**

1. Запустить:
   ```
   python .claude/skills/konspekt/preprocessor.py windows [транскрипт]
   ```
   Получить окна (~2–3K токенов) вокруг потенциальных точек нарезки.

2. Для каждого окна: найти смысловую границу — конец мысли, абзаца.
   - Уверен → зафиксировать таймштамп, продолжить.
   - Не уверен → показать окно пользователю, объяснить неясность, запросить таймштамп.

3. Запустить нарезку:
   ```
   python .claude/skills/konspekt/preprocessor.py split [транскрипт] --at HH:MM:SS,HH:MM:SS,...
   ```
   Получить chunk-файлы (~60 мин каждый, перекрытие ~500 токенов).

---

## Динамический ToV (гибридная стратегия)

Перед Фазой 2 бригадир обеспечивает блок `[TOV_DYNAMIC]`. Логика — гибридная:

1. Имя серии определяется из имени транскрипта.
2. Если существует файл `transcripts/tov_<серия>.md` — взять его содержимое (~1K токенов).
3. Если файла нет — запустить `python .claude/skills/konspekt/preprocessor.py head <транскрипт>`, получить первые ~2500 токенов транскрипта без таймштампов; извлечь характерные обороты, темп, фирменные фразы; записать как блок и **сохранить** его в файл `transcripts/tov_<серия>.md` для следующих частей серии.

Подробности — в `brigadir.md` разделе «Подготовка к Фазе 2».

---

## Фаза 1: Черновая сегментация (параллельно)

Запустить по одному субагенту на каждый чанк, **параллельно**. **Модель каждого субагента — Sonnet** (бригадир остаётся на Opus). В Agent-вызове указать `model: "sonnet"`.

Прочитать `subagent_phase1.md` и `audience_spec.md` (один раз через Read) и встроить их содержимое в промт каждого субагента — субагенты сами Read к этим файлам не делают.

Каждый субагент получает:
- `[CHUNK]` — содержимое своего chunk-файла (или транскрипта целиком, если один чанк)
- `[SERIAL CONTEXT]` — краткий серийный контекст (если есть мастер-MD предыдущих частей)
- `[PROFILE]` — профиль контента

Результат: черновая карта чанка (список сегментов с метаданными).

---

## Бригадир: Глобальный обзор

Прочитать `brigadir.md` и `audience_spec.md`. Выполнить раздел «Глобальный обзор — методолог»:
- Получить черновые карты всех субагентов
- Применить смысловой критерий (один сегмент = одна самостоятельная мысль автора); сигналы смены режима использовать как маркеры потенциальной границы
- При неопределённости — точечно прочитать фрагмент транскрипта
- Выдать чистовую навигационную карту
- Выполнить «Самопроверку — глазами Аудитории (сегментация)» (заменяет старые №1 и №2)

Если режим «с проверкой» — после самопроверки показать чистовую карту пользователю, ждать «ок».

---

## Фаза 2: Написание текста (параллельно)

Перед запуском Ф2 бригадир выполняет **подготовку промтов** (см. brigadir.md раздел «Подготовка к Фазе 2»): один раз читает спеки `subagent_phase2.md`, `tov_spec.md`, `audience_spec.md`; обеспечивает блок ToV через гибридную логику (файл серии или head-выборка); нарезает чанки по сегментам через `preprocessor.py slice`.

Запустить по одному субагенту на каждый сегмент из чистовой карты, **параллельно**. **Модель каждого субагента — Sonnet** (бригадир остаётся на Opus). В Agent-вызове указать `model: "sonnet"`.

Каждый субагент получает в промте (встраивается, не через Read):
- `[INSTRUCTIONS]` — содержимое `subagent_phase2.md`
- `[TOV SPEC]` — содержимое `tov_spec.md` + динамический блок `[TOV_DYNAMIC]`
- `[AUDIENCE]` — содержимое `audience_spec.md`
- `[NAV MAP]` — полная чистовая карта
- `[SEGMENT SPEC]` — запись своего сегмента
- `[CHUNK_сегмента]` — вывод `preprocessor.py slice`

Результат: `**Ключевая мысль:**` + `### Карта` (с метками (И)/(М)/(Д)) + `### Текст` (с `####` подразделами при наличии подтем) + `### Самопроверка` (техническая) для каждого сегмента.

---

## Бригадир: Сборка

Выполнить разделы `brigadir.md` по порядку: «Верификация текста», «Гармонизация стиля», «Переходы между сегментами», «Логическая реконструкция — методолог» (с самопроверкой), «Сборка мастер-MD — редактор», «Финальная самопроверка мастер-MD — глазами Аудитории». После каждой самопроверки бригадира — применить найденные правки до перехода к следующему шагу.

Раздел «Траектория» **заморожен** (см. brigadir.md, FROZEN-комментарий) и не выполняется.

---

## Выходной файл

`transcripts/[название]_мастер.md`

Используется:
- Как источник для Слоя 2 (форматы вывода — отдельный пайплайн)
- Как серийный контекст для будущих сессий вместо перечитывания транскрипта

---

## Слой 2: Виджет

Когда пользователь просит создать виджет из мастер-MD — прочитать `layer2_widget.md` и следовать инструкциям.

Мастер-MD должен уже существовать в `transcripts/` до начала Слоя 2.

---

## Точки остановки

| Точка | Триггер | Действие |
| --- | --- | --- |
| ШАГ 1 нарезка | LLM не уверен в точке реза | Показать окно + объяснить + запросить таймштамп |
| После Фазы 1 | Режим «с проверкой» | Показать чистовую карту, ждать «ок» |
| После сборки | Режим «с проверкой» | Показать мастер-MD, ждать «ок» |

---

## Дисциплина бригадира

Эти правила сокращают накладные расходы бригадира на контекст. Подробнее — в `brigadir.md` раздел «Дисциплина».

1. **В /konspekt не использовать TodoWrite** — пайплайн линейный, прогресс виден из текста.
2. **Не перечитывать чанки после Ф1.** Точечные Read с конкретным таймштампом — только при разрешении сомнений в сегментации.
3. **Не запускать дополнительные deferred tools** (Plan и подобные).
